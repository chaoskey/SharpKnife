# play 模式 JSON 脚本手册

> 本手册完整描述 SharpKnife 的 **play 模式** 中，JSON 脚本文件的格式、九类动作定义、步进执行语义与校验规则。
> 适用版本：与 `SharpKnife.ahk`、`Requirements.md` 第 8 节一致。

---

## 1. play 模式概述

play 模式是 SharpKnife 的一个**独立模式**：

- 它不与 latex / unicode / AI / tikz 四种互斥模式共用切换命令，可与当前生效的互斥模式**并存**。
- 它没有独立的开启 / 关闭操作，状态由**是否绑定脚本文件**自然决定：
  - 已绑定脚本文件 = **开启状态**；
  - 脚本执行完毕自动解绑 = **回到关闭状态**。
- 它拥有独立的 **步进执行命令**（默认 `Ctrl+R`），无论处于哪个状态都有效。
- play 模式**不参与上下文选择与上下文匹配**（步进执行完全依赖脚本文件，与文本上下文无关）。

play 模式的用途：按脚本逐步执行一组动作，例如演示时依次「打字 → 贴图 → 播提示音 → 播视频」等。

---

## 2. 步进执行命令与状态

| 状态 | 触发 `Ctrl+R` 的行为 |
|------|----------------------|
| **关闭**（未绑定脚本） | 弹出系统文件选择框，选定合法脚本后**绑定并立即执行第 1 个动作**；取消选择则无操作 |
| **开启**（已绑定脚本） | 无动作执行中 → 执行**下一步**；已有动作执行中 → 本次无动作 |
| **执行完毕** | 自动解绑回到关闭状态，并在当前光标处字面输出结束语 `【play 脚本结束】` |

- 快捷键可在 `config.ini` 的 `[trigger] step_hotkey` 修改（默认 `^r`，即 `Ctrl+R`）。
- 文件选择框**只允许选择 `.json` 文件**，初始目录为脚本 `SharpKnife.ahk` 所在目录。
- 对话框打开期间再次触发一律忽略（防重入）。
- play 模式**不提供强制停止 / 解绑命令**：只能执行完最后一步后自动解绑；中途换脚本须先执行完当前脚本回到关闭状态再重新触发。

---

## 3. 脚本文件基本规则

### 3.1 文件与编码

- 脚本是 **UTF-8 编码的 JSON 文件**（扩展名 `.json`，存放位置不限，由文件选择框指定）。
- 允许带 UTF-8 BOM（加载时自动去除）。
- 必须是**严格 JSON**：不支持注释、尾随逗号、单引号字符串等非标准写法。

### 3.2 根结构

- 脚本**根是一个动作数组**（相当于一个 `seq`），表示「依次启动」的一串步骤。
- 每个动作都是一个 JSON 对象，用 `type` 字段区分 9 类动作。

### 3.3 通用字段

| 字段 | 是否必填 | 类型 | 说明 |
|------|---------|------|------|
| `type` | 必填 | 字符串 | 动作类型：`text` / `sleep` / `run` / `note` / `paste` / `audio` / `video` / `seq` / `par` |
| `note` | 可选 | 字符串 | 注释说明，仅作人工阅读，**执行时忽略** |

- **未定义的字段一律忽略**（不报错、不校验）：结构校验仅针对已定义字段的必填性与类型，便于日后扩展。

### 3.4 相对路径

- 脚本内的相对路径（`path`）一律以**脚本文件所在目录**为基准解析。
- 绝对路径（形如 `C:\...`、`C:/...`、`\...`）原样使用，不再拼接。

---

## 4. 九类动作详解

### 4.1 `text` —— 文本输入

从**执行动作那一刻的当前焦点窗口**光标处开始写入文本。

| 字段 | 必填 | 类型 | 说明 |
|------|------|------|------|
| `value` | 必填 | 字符串 **或** 字符串数组 | 要写入的文本 |

**字符串形态**（适合短文本，单行写法）：

```json
{ "type": "text", "value": "第一行{ENTER}第二行{BS 9}" }
```

**数组形态**（适合大段多行文本）：每个元素代表一行，执行时按序输出并在元素之间自动插入换行；数组元素必须都是字符串。

```json
{ "type": "text", "value": ["第一行", "第二行{ENTER}", "第三行"] }
```

**文本解析规则**（对字符串与数组的每个元素均适用）：

1. `\n` 或 `\r`（或 `\r\n`）→ 换行（等价于一次回车）。
2. 形如 `{...}` 的部分按 **AHK 键名**解释：
   - `{键名}`，如 `{ENTER}`（回车）；
   - `{键名 次数}`，如 `{BS 9}`（退格 9 次）、`{Left 5}`（左移 5 格）。
   - 键名为字母 / 数字组成的单个标识，不区分大小写，可带一个可选「空格 + 数字」重复次数。
3. **其余字符一律按字面原样输出**——包括 `+` `!` `#` `^` `%` 等修饰符，不会被解释为修饰键。
4. 字面 `{` / `}` 用反引号 `` ` `` 前缀转义：`` `{ `` 输出 `{`，`` `} `` 输出 `}`。

> 说明：键名仅支持「单个字母数字标识 + 可选 ` 重复次数`」形式。多词键名（如 `{Ctrl Down}`）不会命中键名规则，会被当作字面文本原样输出。

### 4.2 `paste` —— 贴图

把图片按指定尺寸缩放后复制到剪贴板，经 **Snipaste 贴图**展示。

| 字段 | 必填 | 类型 | 默认 | 说明 |
|------|------|------|------|------|
| `path` | 必填 | 字符串 | — | 图片文件路径（PNG 等） |
| `pos` | 可选 | `[x, y]` | 屏幕居中 | 贴图窗口左上角屏幕坐标 |
| `size` | 可选 | `[w, h]` | 原图尺寸 | 显示尺寸；指定后拉伸到该精确尺寸（**不保持宽高比**） |
| `opacity` | 可选 | 数字 0~100 | 100 | 不透明度；越界按最近边界截断 |

```json
{ "type": "paste", "path": "images/fig1.png", "pos": [100, 200], "size": [400, 300], "opacity": 80 }
```

行为要点：

- 缺省 `pos` 时在主显示器屏幕居中；`size` 缺省用原图尺寸。
- 依赖本机已安装 **Snipaste**（未运行时自动启动并等待就绪）。
- `path` 不存在 → 判执行失败，跳过继续。

### 4.3 `audio` —— 播放音频

| 字段 | 必填 | 类型 | 默认 | 说明 |
|------|------|------|------|------|
| `path` | 必填 | 字符串 | — | 音频文件路径 |
| `start` | 可选 | 时间 | 0 | 起播时间（见 §7 时间格式） |
| `end` | 可选 | 时间 | 播到结尾 | 结束时间 |
| `volume` | 可选 | 数字 | 1.0 | 音量增益倍数；负值截断为 0 |
| `wait` | 可选 | 布尔 | `false` | 是否等待播放结束 |

```json
{ "type": "audio", "path": "audio/bgm.mp3", "start": "00:10", "end": "00:30", "volume": 0.8, "wait": true }
```

行为要点：

- 实现：`ffplay -nodisp`（无界面）+ `-ss 起播时间` + `-t 时长`（`时长 = end − start`）+ `-af volume=音量`。
- `wait = false`（默认）：**启动即算完成**，后台继续播放，不阻塞步进。
- `wait = true`：**非阻塞等待**——启动后立即返回，跟踪 ffplay 进程退出判定完成（进程退出即视为完成，不深究退出原因），期间占用执行中标记。
- `path` 不存在 → 判执行失败，跳过继续。

### 4.4 `video` —— 播放视频

| 字段 | 必填 | 类型 | 默认 | 说明 |
|------|------|------|------|------|
| `path` | 必填 | 字符串 | — | 视频文件路径 |
| `pos` | 可选 | `[x, y]` | 播放器默认（通常居中） | 播放窗口左上角坐标 |
| `size` | 可选 | `[w, h]` | 原始尺寸 | 窗口尺寸；画面在窗口内**等比缩放**（保持宽高比）。`w` 或 `h` 可为 `0` 表示**不设该维度**（至少一个 > 0） |
| `opacity` | 可选 | 数字 0~100 | 100 | 不透明度；越界截断 |
| `start` / `end` | 可选 | 时间 | 0 / 播到结尾 | 起止时间（同 `audio`） |
| `volume` | 可选 | 数字 | 1.0 | 音量增益倍数（同 `audio`） |
| `wait` | 可选 | 布尔 | `false` | 是否等待播放结束 |

```json
{ "type": "video", "path": "video/demo.mp4", "pos": [300, 200], "size": [800, 450], "opacity": 100, "wait": true }
```

行为要点：

- 实现：`ffplay` + `-left` / `-top` / `-x` / `-y` 指定窗口位置尺寸，`-ss` / `-t` / `-af volume=` 同 `audio`，透明度经 `WinSetTransparent` 设置。
- `size` 支持「只设一边」：`[w, 0]` 只设宽度（`-x`），`[0, h]` 只设高度（`-y`）；`0` 表示该维度不设、用原始尺寸，但 `w`、`h` **不能同时为 0**。
- `wait = false`：启动即算完成；`wait = true`：非阻塞等待——跟踪 ffplay 进程退出**或播放窗口关闭**判定完成。
- 视频窗口会**激活抢焦点**，执行后自动恢复焦点到原窗口（保证后续 `text` 输出到正确位置）。
- `path` 不存在 → 判执行失败，跳过继续。

### 4.5 `seq` —— 顺序嵌套动作

| 字段 | 必填 | 类型 | 默认 | 说明 |
|------|------|------|------|------|
| `actions` | 必填 | 动作数组 | — | 子动作列表（空数组合法但立即完成） |
| `step` | 可选 | 布尔 | `true` | 单步 / 一次性 |

```json
{ "type": "seq", "step": true, "actions": [ <动作>, <动作>, ... ] }
```

- 子动作**始终按次序串行**执行（前一个完成再启动下一个）；`step` 只控制**一次步进推进多少个子动作**：
  - `step = true`（默认，**单步**）：一次步进命令只执行第 1 个子动作。
  - `step = false`（**一次性**）：一次步进命令依次执行完所有子动作。
- **继承规则**：一次性 `seq` 的**所有嵌套子 `seq`（任意深度）都强制一次性**，忽略其自身的 `step`。

### 4.6 `par` —— 并行嵌套动作

| 字段 | 必填 | 类型 | 说明 |
|------|------|------|------|
| `actions` | 必填 | 动作数组 | 子动作列表（空数组合法但立即完成） |

```json
{ "type": "par", "actions": [ <动作>, <动作>, ... ] }
```

- **并行启动**所有子动作，**全部完成后**该 `par` 才算完成；`par` **无单步**。
- **继承规则**：`par` 的**所有嵌套子 `seq`（任意深度）都强制一次性**，忽略其 `step`。

### 4.7 `sleep` —— 延时等待

等待指定时长后继续（用于演示节奏控制：贴图停留、段落间停顿等）。

| 字段 | 必填 | 类型 | 默认 | 说明 |
|------|------|------|------|------|
| `duration` | 必填 | 时间 | — | 等待时长（见 §7 时间格式） |

```json
{ "type": "sleep", "duration": 2 }
```

行为要点：

- 等待期间占用执行中标记（此时步进命令无动作）。
- `duration` 为 0 视为立即完成；为负 → 结构非法。

### 4.8 `run` —— 启动程序 / 打开文档 / 网址

启动一个程序、打开一个文档，或打开一个 URL。

| 字段 | 必填 | 类型 | 默认 | 说明 |
|------|------|------|------|------|
| `path` | 必填 | 字符串 | — | 程序 / 文档路径，或 URL（相对路径以脚本目录为基准） |
| `args` | 可选 | 字符串 | — | 命令行参数（原样追加到 `path` 之后） |
| `wait` | 可选 | 布尔 | `false` | 是否等待进程退出 |
| `hide` | 可选 | 布尔 | `false` | 是否隐藏窗口启动 |

```json
{ "type": "run", "path": "https://example.com" }
{ "type": "run", "path": "tools/demo.exe", "args": "--fullscreen", "wait": true }
```

行为要点：

- `wait = false`（默认）：启动即算完成，后台继续运行，不阻塞步进。
- `wait = true`：非阻塞等待——启动后立即返回，跟踪进程退出判定完成（进程退出即视为完成，不深究退出原因），期间占用执行中标记。
- 本地 `path` 不存在 → 判执行失败，跳过继续；URL（`http://` / `https://` / `mailto:` / `www.` 开头）不检查本地文件。
- `hide = true` 时隐藏窗口启动（适合后台命令）；对 URL 无效。
- 启动程序会抢焦点，执行后自动恢复焦点到原窗口。

### 4.9 `note` —— 屏幕提示

弹出短暂文字提示（系统气泡，屏幕左下角附近），用于演示中的解说、提醒。

| 字段 | 必填 | 类型 | 默认 | 说明 |
|------|------|------|------|------|
| `text` | 必填 | 字符串 | — | 提示文字（可用 `\n` 换行） |
| `duration` | 可选 | 时间 | 2.5 | 显示时长（秒；到点自动消失并继续） |

```json
{ "type": "note", "text": "接下来展示核心公式推导", "duration": 3 }
```

行为要点：

- 提示显示 `duration` 秒后自动消失，然后继续下一步；期间占用执行中标记。
- 实现基于系统气泡提示（`ToolTip`），无需额外依赖。

---

## 5. 字段速查表

| 字段 | 适用动作 | 类型 | 必填 | 默认 | 说明 |
|------|---------|------|------|------|------|
| `type` | 全部 | 字符串 | ✔ | — | 动作类型 |
| `note` | 全部 | 字符串 | ✘ | — | 注释（执行时忽略） |
| `value` | text | 字符串 / 字符串数组 | ✔ | — | 文本内容 |
| `text` | note | 字符串 | ✔ | — | 提示文字 |
| `path` | run / paste / audio / video | 字符串 | ✔ | — | 文件 / 程序路径（相对脚本目录解析） |
| `args` | run | 字符串 | ✘ | — | 命令行参数 |
| `duration` | sleep / note | 时间 | sleep ✔ / note ✘ | 2.5（note） | 等待 / 显示时长 |
| `pos` | paste / video | `[x, y]` | ✘ | 居中 / 播放器默认 | 窗口位置 |
| `size` | paste / video | `[w, h]` | ✘ | 原尺寸 | paste 拉伸不保比例（w、h 均需 >0）；video 等比缩放，0=不设该维度（至少一个 >0） |
| `opacity` | paste / video | 数字 0~100 | ✘ | 100 | 不透明度，越界截断 |
| `start` | audio / video | 时间 | ✘ | 0 | 起播时间 |
| `end` | audio / video | 时间 | ✘ | 播到结尾 | 结束时间 |
| `volume` | audio / video | 数字 | ✘ | 1.0 | 音量增益，负值截断为 0 |
| `wait` | run / audio / video | 布尔 | ✘ | false | 是否等待完成 / 播放结束 |
| `hide` | run | 布尔 | ✘ | false | 是否隐藏窗口启动 |
| `actions` | seq / par | 动作数组 | ✔ | — | 子动作列表 |
| `step` | seq | 布尔 | ✘ | true | 单步 / 一次性 |

---

## 6. 校验规则（加载阶段整体执行一次）

加载脚本时一次性完成全部校验，分两类结果：

**① 结构非法 → 整体拒绝、不绑定（弹错误提示，保持关闭状态）：**

- 根不是动作数组；
- 动作缺必填字段、字段类型错误、`type` 未知；
- `pos` / `size` 数组长度不是 2 或元素非数字；
- `size` 的宽或高为负（`video` 允许为 `0` 表示不设该维度，但宽高不能同时为 `0`；`paste` 宽高均须 > 0）；
- `start` / `end` / `duration` 时间格式非法或为负；
- JSON 无法解析（语法错误、非法转义、尾部多余内容等）。

**② 数值越界 → 截断到最近边界（clamp），不视为非法：**

- `opacity` 超出 0~100 → 截断到 `[0, 100]`；
- `volume` 为负 → 截断为 0。

---

## 7. 时间格式（`start` / `end` / `duration`）

支持以下任一写法：

| 写法 | 含义 | 示例 |
|------|------|------|
| 秒数 | 纯数字（整数 / 浮点） | `5`、`2.5` |
| `MM:SS` | 分:秒（可带小数秒） | `"01:30"`、`"00:10.5"` |
| `HH:MM:SS` | 时:分:秒（可带小数秒） | `"00:01:30"` |

- `start` 缺省为 `0`（不起播偏移）。
- `end` 缺省为「播到结尾」。
- 若 `end < start`，实际播放时长取 0。

---

## 8. 步进（step）执行语义

脚本维护两个状态：

- **步进游标栈**：栈底为**顶层游标**（指向下一个待执行的顶层动作）；进入单步 `seq` 时压入其**内部游标**，该 `seq` 子动作全部执行完再弹出。
- **执行中标记**：当前是否有动作在执行、尚未完成。

流程：

1. **绑定即执行第 1 个动作**（无需再按步进命令）；顶层数组为空（`[]`）时绑定后立即自动解绑。
2. 每次触发步进命令（`Ctrl+R`）：
   - 执行中标记为真 → 本次无动作；
   - 否则启动栈顶游标指向的动作：
     - `text` / `paste`：立即执行（期间标记为真，完成前移游标）；
     - `sleep`：等待 `duration` 秒后完成（期间标记为真）；
     - `run`：依 `wait` 决定——`false` 启动即完成；`true` 非阻塞等待进程退出（期间标记为真）；
     - `note`：显示提示 `duration` 秒后自动消失并完成（期间标记为真）；
     - `audio` / `video`：依 `wait` 决定——`false` 启动即完成；`true` 非阻塞等待播放结束（期间标记为真）；
     - `seq` 单步（`step=true`）：只启动第 1 个子动作并压入内部游标（`seq` 自身不占标记，子动作全部完成才弹出、顶层游标前移）；
     - `seq` 一次性 / `par`：依次 / 并行执行完所有子动作（期间标记为真，嵌套子 `seq` 强制一次性）。
3. 顶层游标越过最后一个动作 → 执行完毕、自动解绑、回到关闭状态，并在光标处字面输出 `【play 脚本结束】`。

> 注：`audio` / `video` 是否阻塞**仅由 `wait` 决定**（与是否嵌套无关）。`wait=false` 后台继续播放、不阻塞步进；「播完提示音再打字」可把两者按序放入同一 `seq`，并给提示音设 `wait=true`。解绑不影响已在后台播放的 `audio` / `video`（继续播完）。

---

## 9. 失败处理

- **脚本加载失败**（JSON 无法解析或结构 / 字段非法）→ 弹出错误提示、**不绑定**（保持关闭状态）。
- **动作执行失败**（文件 / 程序不存在、ffplay / Snipaste 未安装、贴图失败等）→ 记日志 / 提示后**跳过该动作**继续前进，不中断脚本；此规则同样适用于 `seq` / `par` 的子动作。

---

## 10. 完整示例

```json
[
  { "type": "text", "value": "大家好，欢迎来到演示{ENTER}" },
  { "type": "note", "text": "接下来展示标题图片", "duration": 3 },
  { "type": "sleep", "duration": 1 },
  { "type": "run", "path": "https://example.com" },
  { "type": "paste", "path": "images/title.png", "pos": [100, 100], "size": [600, 200] },
  {
    "type": "seq",
    "step": false,
    "note": "先放提示音，提示音结束后自动打字（step=false：一次步进依次执行完；audio 设 wait=true 等播完再打字）",
    "actions": [
      { "type": "audio", "path": "audio/ding.mp3", "wait": true },
      { "type": "text", "value": "（提示音结束）开始讲解" }
    ]
  },
  {
    "type": "par",
    "note": "背景音乐与演示视频同时播放",
    "actions": [
      { "type": "audio", "path": "audio/bgm.mp3", "volume": 0.5 },
      { "type": "video", "path": "video/demo.mp4", "pos": [400, 200], "size": [800, 450] }
    ]
  },
  {
    "type": "seq",
    "step": false,
    "note": "外层一次性 seq：其嵌套的子 seq 即使写 step=true 也强制一次性依次执行完",
    "actions": [
      {
        "type": "seq",
        "step": true,
        "note": "此处的 step=true 被外层一次性 seq 覆盖，实际按一次性执行",
        "actions": [
          { "type": "text", "value": "第 1 段" },
          { "type": "text", "value": "第 2 段" }
        ]
      },
      { "type": "text", "value": "第 3 段" }
    ]
  }
]
```

> 说明：JSON 键名（`type` / `note` / `text` / `path` / `value` / `args` / `duration` / `hide` / `actions` / `step` / `wait` / `pos` / `size` / `opacity` / `start` / `end` / `volume`）采用英文（与 `config.ini` 一致）；动作内容（如 `value` 与示例中的中文）可自由使用中文。示例中的 `seq` 显式写 `"step": false` 表示一次性依次执行完；省略 `step` 或写 `"step": true` 则为单步（默认）——一次步进只执行该 `seq` 的第一个子动作。`audio` / `video` 省略 `wait` 或写 `"wait": false` 表示启动即算完成，写 `"wait": true` 表示等待播放结束。最后一个动作演示了继承规则：**外层一次性 `seq`（`step=false`）嵌套的子 `seq` 即便写 `step=true`，也被强制一次性依次执行完**（`par` 的嵌套子 `seq` 同理）。

---

## 11. 常见问题与注意事项

- **JSON 必须是严格 JSON**：不要写注释、尾随逗号、单引号字符串。
- **`{` `}` 与键名的区别**：`value` 里只有形如 `{键名}` 或 `{键名 次数}` 才按键名解释，其余 `{...}` 当字面文本；要输出字面 `{` / `}` 用 `` `{ `` / `` `} `` 转义。
- **修饰符安全**：`+` `!` `#` `^` `%` 一律字面输出，不会被当作修饰键，无需转义。
- **相对路径基准**：以脚本文件所在目录为准，不是 `SharpKnife.ahk` 所在目录。
- **依赖程序**：`paste` 依赖 Snipaste；`audio` / `video` 依赖 ffmpeg 的 `ffplay`；`run` 依赖目标程序 / 文档存在；`sleep` / `note` 无外部依赖。未安装 / 不存在时动作执行失败但会跳过继续，不会中断脚本。
- **焦点恢复**：`paste` / `run` / `audio` / `video` 会弹窗 / 启动程序抢焦点，引擎会自动恢复焦点到原窗口，保证后续 `text` 输出到正确位置。
- **无强制停止**：play 模式只能自然执行完自动解绑，不提供中途停止 / 解绑命令。
- **执行完毕结束语**：脚本执行完（含空脚本）会在光标处字面输出 `【play 脚本结束】`。
