# play 模式 —— AI 复现提示语



### AI 提示语

为 ·`AutoHotkey v2` 工具新增独立模式 **play**：不参与 `latex/unicode/AI/tikz` 的切换与上下文选择/匹配，可与当前互斥模式并存；由步进执行命令（默认 `Ctrl+R`，可配置）驱动，无独立开关，状态由**是否绑定脚本文件**决定，且**只能执行完最后一步自动解绑**、无强制停止。

**命令三态**：①未绑定——弹出仅限 `.json` 的系统文件选择框（初停主脚本目录，开启期间重复触发忽略），选入并经校验通过即**绑定并立即执行第 1 步**，取消则无动作；②已绑定——空闲时执行**下一步**，上一动作未完成时本次无动作；③执行完毕——自动解绑，并在当前光标处**字面输出**结束语 `【play 脚本结束】`；空脚本 `[]` 绑定即解绑、同样输出。

**脚本文件**：`UTF-8`（可带 `BOM`）严格 `JSON`；根为动作数组（即顶层顺序序列），每动作以 `type` 分 9 类，可带仅供阅读的 `note` 字段，**未定义字段一律忽略**；相对路径以脚本所在目录为基准，绝对路径原样用。

**9 类动作**：

1. `text` 打字——在光标处写入 `value`（单串或多行串，多行串逐行插换行）；`\n`/`\r`→回车，`{键名}`、`{键名 次数}` 按 `AHK` 键名发送，其余字符（含修饰符）一律字面原样输出，字面 `{`/`}` 用反引号转义，多词键名当普通文本。
2. `paste` 贴图——把 `path` 图片按 `size` 缩放复制到剪贴板经 **`Snipaste`** 贴出；`pos` 缺省屏幕居中，`size` 缺省原图（`0`=不设该维度，只设一边按原图等比，两边都设则拉伸不保比例），`opacity` 0~100 缺省 100、越界收敛。
3. `audio` 播音——`ffplay` 播放 `path`，可设 `start`/`end` 起止、`volume`（负值归零）；`wait=false` 启动即算完成后台播，`wait=true` 非阻塞等播完（占用执行中标记）。
4. `video` 播视频——ffplay 播放 `path`，可设窗口 `pos`/`size`（画面等比，宽高不能同设 0）、`opacity`、`start`/`end`/`volume`、`wait`；`wait=true` 非阻塞等播完或关窗；会抢焦点、播后自动恢复。
5. `seq` 顺序——子动作 `actions` 按序串行；`step` 缺省 true=单步（一次只做第 1 个）、false=一次性（一次做完）；**继承**：一次性 seq 的所有嵌套子 seq 强制一次性。
6. `par` 并行——`actions` 并行启动、全部完成才算完成、无单步；**继承**：嵌套子 seq 均强制一次性。
7. `sleep` 延时——等 `duration` 后继续（占用执行中标记）；`0` 立即完成、负值非法。
8. `run` 启动——`path` 运行程序/打开文档或 URL，可带 `args` 参数、`wait`（false=启动即完成，true=非阻塞等退出）、`hide`（隐藏启动，对 URL 无效）；抢焦点、后恢复。
9. `note` 提示——屏幕气泡显示 `text`，`duration` 秒（缺省 2.5）后自动消失并继续（占用执行中标记）。

**时间格式**（`start`/`end`/`duration`）：纯数字秒数、`MM:SS`、`HH:MM:SS`（均可带小数）；`start` 缺省 0、`end` 缺省播完、`end<start` 取 0。

**步进语义**：维护**步进游标栈**（栈底为顶层游标指向下一待执行动作；进入单步 seq 压入其内部游标、子动作做完再弹出）与**执行中标记 busy**。绑定即执行第 1 步；此后每次触发：busy 为真则本次无动作，否则启动栈顶——即时类立即执行，耗时动作按 `wait`/时长决定是否等到完成（等待期间 busy 为真），单步 seq 只启动第 1 个子动作并压栈（自身不占标记），一次性 seq/par 一次做完。顶层游标越界即自动解绑并输出结束语。注：`audio`/`video` 只按 `wait` 决定是否阻塞（与嵌套无关），解绑不影响后台播放。

**校验**（加载时做一次）：结构非法（根非数组、缺必填、类型错、`type` 未知、`pos`/`size` 非两数字、`size` 负或宽高同 0、时间非法或负、JSON 无法解析）→ 整体拒绝、不绑定并报错；`opacity`/`volume` 越界 → 按最近边界收敛、不视为非法。

**失败处理**：动作执行失败（文件/程序缺失、依赖未装、贴图失败等）→ 提示后**跳过继续**、不中断脚本（seq/par 子动作同理）。

**工程约束**：依赖 Snipaste（paste）、ffplay（audio/video）、目标存在（run）；未装/缺失→失败跳过。弹窗/启动动作会抢焦点，需记录并恢复焦点到原窗口。代码注释与运行时文字全用中文；默认关闭的调试日志开关；内置可去 BOM、支持转义的 JSON 解析器；`text` 逐字符字面输出（受打字延迟控制）。
