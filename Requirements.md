> 一个基于 AutoHotkey v2 的全局 **LaTeX 命令 / Unicode 符号** 快速补全工具。在任何文本编辑框中按下 `Ctrl+J`，即可把光标前刚输入的 LaTeX 键补全为完整的 LaTeX 命令、Unicode 字符或环境模板。默认情况下所有数据来自本地触发表 `latexs.cvs`，**不联网、不需要任何 API**。当然如果切换到 AI 模式，还是需要的哦！
> 
> 本文附件有 latexs.cvs ， 但不提供实际代码，因为原则上用本文作为 AI 提示语，至少用 DeepSeek V4 Flash 可以生成代码。就算没能一次性测试通过，我相信通过简单几轮人工交互，最终也能生成测试通过的代码（事实上我就是这样做的）。

### 1. 验证环境：

Win10 +  **AutoHotkey v2**（[https://www.autohotkey.com/](https://www.autohotkey.com/) ，必须是 v2，不是 v1）

如果期望启用 tikz 模式，那么还需要安装： miktex + ghostscript + snipaste  

### 2. 整体逻辑

- *触发命令*  》  *上下文选择* (非法上下文则直接返回，无操作)  》  *上下文匹配*  （没有匹配上则直接返回，无操作） 》 *动作触发* （如果取消则直接返回，无操作）

- *循环切换命令* 》 *模式切换*   （latex（默认） → unicode → AI → tikz → latex）

- *直接切换命令* 》 *模式切换*   （直接切换到指定模式）

- *触发模式列表* 》 弹出无边框列表 》 *通过上下键移动选择进行模式切换*，或 *鼠标点击目标模式直接切换*   （直接切换到指定模式）

- *步进执行命令* 》 *执行一步*   （play 模式专属；无论处于哪个状态都有效，关闭状态下弹出脚本文件选择窗口，开启状态下执行脚本的下一步）

- *径向菜单触发* 》 径向菜单**开/关切换**：未打开时弹出径向菜单，已打开时关闭径向菜单

- *模式关系* ： latex / unicode / AI / tikz 四种模式**互相排它**，有且仅有其中一种当前有效，它们共享同一个 *触发命令*（Ctrl+J）； play 模式是**独立模式**，可与上述四种模式之一**并存**，拥有独立的 *步进执行命令*（Ctrl+R）。 play 模式没有独立的开启/关闭操作，其状态由**是否绑定脚本文件**自然决定：已绑定脚本文件即处于开启状态，脚本执行完毕自动解绑即回到关闭状态。

### 3. 命令

- 触发命令 ：  Ctrl+J  （可通过配置修改； latex / unicode / AI / tikz 四种互斥模式共享）

- 循环切换命令 ： Ctrl+Shift+J   （可通过配置修改；仅在四种互斥模式之间循环）

- 直接切换命令 ： Ctrl+Shift+<数字>   （<数字>之前的前缀可通过配置修改， 0=latex，1=unicode，2=AI，3=tikz）

- 触发模式列表 ： Ctrl+Shift+\   （可通过配置修改）

- 步进执行命令 ： Ctrl+R   （可通过配置修改；play 模式专属，无论处于哪个状态都有效；关闭状态下还可通过配置 `[play] script_path` 预置脚本全路径、省略文件选择框，见第 7 节）

### 4. 模式切换

触发模式 ： 

- 循环切换 :  **latex 模式** （默认模式）； **unicode 模式**  ；**AI 模式**  ；**tikz 模式**  。 这四种模式**互相排它**，有且仅有其中一种当前有效； *切换命令*  在这四个模式之间循环切换。

- 直接切换 :  数字映射 0=latex，1=unicode，2=AI，3=tikz

- 触发模式列表选择切换 : 完整列表 latex 模式（0），unicode 模式（1），AI 模式（2），tikz 模式（3）——既可用上下键移动选择 + 回车切换，也可直接鼠标点击目标模式切换（Esc 取消则无操作）

- play 模式 :  **独立模式**，不与上述四种互斥模式共用切换命令；它可与当前生效的互斥模式**并存**，拥有独立的 *步进执行命令*（Ctrl+R）。 play 模式没有独立的开启/关闭操作，其状态由**是否绑定脚本文件**自然决定：已绑定脚本文件即处于开启状态，脚本执行完毕自动解绑即回到关闭状态。

### 5. 上下文选择

触发后的上下文选择 ： 文字光标前的非空连续字符串 。

也可以在触发前人工选择指定内容作为上下文，然触发后直接用选择的上下文进行后续的匹配。

### 6. 上下文匹配

如果是 AI 模式 和 tikz 模式 ，那么不存在上下文匹配，任何非空上下文都都是合法的。并且直接用这个非空上下文作为 AI 提示语。

play 模式 不参与上下文选择与上下文匹配（步进执行完全依赖自定义格式脚本，与文本上下文无关）。

下面的 上下文匹配 仅仅针对 latex 模式  和  unicode 模式。

基于 触发表 ： latexs.cvs 的查表匹配。此表中也有详细注释。

unicode 模式 的触发表过滤 ： 第二个字段的字符串中间不能含 空格。

latex 模式 的 触发表过滤 ：第二个字段的字符串不能以 ：开头。

合法上下文： `<前缀><待匹配字符串>`  。其中`<前缀>`  可能是  `_`   ,  `^`  , `_\`   ,  `^\`  ,  `\` ；  `<待匹配字符串>` 代表   非空的  英文字母 或 数字 或 非 _ ^ \ 的符号 组成的区分大小写的字符串。   

对非法上下文触发的结果就是： 直接返回，无操作 。

合法上下文基于触发表第一个字段的匹配模式  ：`<前缀>*<待匹配字符串>*`  。   其中   `*`  代表  非空的  英文字母 或 数字 或 非 _ ^ \ 的符号 组成的区分大小写的字符串 或 空字符串。

上下文匹配结果只有四种可能类型：1）精确匹配  =（两个  `*`  都是空字符串）；2）头对齐匹配 `<`（第一个  `*`  是空字符串，第二个  `*`  是非空字符串）；3）尾对齐匹配 `>`（第一个  `*`  是非空字符串，第二个  `*`  是空字符串）；4）中间匹配 `~`（两个  `*`  都是非空字符串）。

完整的匹配结果：1）如果只匹配出一项，那么直接 *动作触发* ；2）如果匹配出两项或更多，那么先弹出一个无框列表，这个列表最多显示10项，但通过上下键移动，可以看见所有匹配项，然后回车选择实际要触发的项进行  *动作触发*。 在选择触发之前，也可以按取消键，取消触发。 此外，也可以直接用鼠标或笔点击列表中的某一项（含当前已高亮的那一项）来直接选择该项并触发——点哪项就选哪项；原有的上下键移动、回车选择、取消键取消等操作方式保持不变。

关于无框列表的的各项的内容格式：<匹配类型> <第一个字段字符串> <第二字段字符串> 。 其中 <匹配类型> 形如： = > < ~ ； <第二字段字符串> 要剔除 : 前缀。

### 7. 动作触发

latex 模式的触发有两种情况：  
- 1） 只有两个字段时，触发结果就是： 用第一个字段的字符串 替换 上下文字符串，并且尾部补一个空格。  

- 2）有三个字段时，触发结果就是：用第三个字段解析后的字符串 替换 上下文字符串，并且将光标左移指定格数。

unicode 模式的触发结果：  用第二个字段的字符串（剔除掉 `:` 前缀） 替换 上下文字符串，并且尾部补一个空格。

AI 模式的触发结果：将 用 AI 生成的结果 放在 上下文字符串 后面， 这两个内容要隔一行 。 是否采用流式请求由配置决定；代码在缺省值层面支持非流式回退，而仓库提交的 `config.ini.example` 当前示例值为流式。 如果是思考模式 并且 流式请求的前提下，务必弹出一个合适长宽无边框窗口，将思考过程在这个窗口中滚动呈现，思考完毕后，自动离开这个窗口（不用关闭这个窗口，但可以随时人工回到这个窗口，通过 Esc 键关闭）。然后，进行正式结果输出。

思考过程展示窗口（思考模式 且 流式请求时弹出）还须满足以下人性化要求：
- 1）务必等思考过程**完整展示完毕后**，再输出正式结果（不得在思考过程还在滚动呈现时就提前输出结果，也不得因超时而截断思考展示）；
- 2）思考窗口必须**可拖动**（用户可把它移到不遮挡编辑区域的位置）；
- 3）思考过程中窗口置顶以确保可见；思考完毕自动离开后，窗口**不再置顶**，当焦点回到编辑区域时窗口应被编辑器盖住（不可见但仍存在，除非已被用户手工关闭）；用户可随时通过任务栏回到该窗口并按 Esc 关闭。

  AI 生成的结果有如下规范性要求：

- 1）默认情况下，必须是完整的 latex 片段或范例 （也就是可以合法渲染成数学公式或者符号）；
- 2）如果上下文提示语中有特别的要求，那么返回的结果可以也是 unicode 符号，或者 由 unicode 符号组成的公式。
- 3）如果上下文提示语要求输出的内容是 markdown 格式，那么务必确保 行内公式 用一对 `$` 包围，而行间公式用一对 `$$` 包围。
- 4）如果上下文提示语要求输出绘图代码，那么采用　MikTex + TikZ，并且如果输出包括 `\begin{document}`，那么必须用 用 standalone 文档类；如果是3D输出，那么优先采用 `tikz-3dplot` 宏包，具体根据上下文提示语涉及任务，也可以改用 `pgfplots` 或 纯 TikZ 的 `3d` 库 。
- 5）如果满足上下文提示语要求的生成结果有2种或多种可能，那么务必弹出一个无框列表（相关的要求和后续操作都和前面曾经提及的无框列表是一样的），提供给用户选择。

tikz 模式的触发结果：把上下文内容（选区优先，其次光标前内容）视为 TikZ 绘图代码，自动编译渲染成图片并且默认通过 snipaste 进行贴图（纯本地，不联网；依赖本机已安装的 TeX 发行版与 PDF 转 PNG 工具）。

- 1）渲染链路：pdflatex（MiKTeX / TeX Live）编译 → PDF 转 PNG（自动探测 pdftoppm / mutool / gswin64c / magick，也可在配置中指定）→ snipaste 贴图。
- 2）绘图代码的包装：自动判断上下文内容的形态并包装成最小可编译文档：
  - 包含 `\begin{document}` → 视为完整文档，原样编译；
  - 包含 `\begin{tikzpicture}` → 用 standalone 文档类 + `\usepackage{tikz}` 包装；
  - 仅裸绘图语句（如 `\draw (0,0) -- (1,1);`）→ 额外包一层 `\begin{tikzpicture}...\end{tikzpicture}`；
  - 形如 `\usepackage` / `\usetikzlibrary` / `\tikzset` / `\pgfplotsset` 开头的行会被自动提取到文档导言区（避免写在 document 体内编译报错）。
- 3）编译要求：`-interaction=nonstopmode -halt-on-error`；编译失败时在无框窗口中显示错误信息（取自 main.log 的错误行），不显示图片；编译带超时保护。
- 4）工具路径、渲染分辨率（DPI）、边框留白、附加宏包、编译超时等参数在 config.ini 的 `[tikz]` 段配置；工具路径留空则自动探测（PATH 或常见安装路径）。
- 5）编译期间显示进度提示；结束后自动清理临时文件。
- 6）务必保留触发的调试日志（如果调试开关时开的话）。

play 模式的触发结果：按绑定脚本的**自定义格式脚本**步进式执行（脚本格式与步进语义详见第 8 节）。play 模式无独立开关，其状态由**是否绑定脚本文件**自然决定——已绑定即开启，脚本执行完最后一步自动解绑回到关闭状态；*步进执行命令*（Ctrl+R）在两种状态下均有效。

- **play 脚本的指定方式**（二选一，**仅是选择脚本的途径不同**；选定后绑定、步进、自动解绑、失败处理等全部语义完全一致）：
  - **手动选择（默认）**：配置 `[play] script_path` **未设置**（留空）时，保持原有行为——关闭状态下触发弹出**系统文件选择对话框**（`FileSelect`，过滤 `*.json`，初始目录 = 脚本 `SharpKnife.ahk` 所在目录），由用户选定 `.json` 脚本；
  - **配置预置**：将 `config.ini` 的 `[play] script_path` 设置为 play 脚本的**全路径**时，关闭状态下触发**不再弹出选择框**，直接绑定该脚本。若该配置路径的脚本**不存在或加载失败**，**不绑定**（保持关闭状态，原因记入调试日志并作非阻塞提示），并**回退到手动选择**（弹出文件选择对话框）以便用户另行选择，保证功能始终可用。
- **关闭状态下触发**（脚本选定后）：绑定并进入开启状态，且**立即执行第 1 个动作**。手动选择时取消选择（空手关闭）则不绑定、无动作；所选脚本顶层动作数组为空（`[]`）时绑定后立即自动解绑、无动作。对话框已打开期间再次触发一律忽略（防重入）。
- **开启状态下触发**：无动作执行中则执行**下一步**（步进游标前移一位）；已有动作执行中则本次**无动作**。play 模式**不提供强制停止 / 解绑命令**，只能执行完最后一步后自动解绑；中途更换脚本须先执行完当前脚本回到关闭状态再重新触发。
- **执行完毕（自动解绑）时**：自动解绑回到关闭状态。
- **失败处理**：脚本加载失败（JSON 无法解析或结构/字段非法）→ 弹出错误提示、**不绑定**（保持关闭状态）；动作执行失败（如文件不存在、ffmpeg / Snipaste 未安装）→ 记日志 / 提示后**跳过该动作**继续前进，不中断脚本；此规则同样适用于 `seq` / `par` 的子动作。

### 8. play 脚本文件格式与动作定义

play 模式的脚本文件是一个 **UTF-8 编码的 JSON 文件**（扩展名 `.json`；存放位置不限，由 *系统文件选择对话框* 指定，且该对话框**只允许选择 `.json` 文件**）。脚本内的相对路径（如 `path`）一律以**脚本文件所在目录**为基准解析。

- 脚本的**根**是一个 **seq（顺序动作）**：顶层是一个动作数组，表示"依次启动"的一串步骤。
- 每个动作都是一个 JSON 对象，用 `type` 字段区分 9 类动作：`text` / `paste` / `audio` / `video` / `seq` / `par`（基础 6 类），外加 `sleep` / `run` / `note`（辅助 3 类，见 8.3）。
- 所有动作对象都可携带一个可选的 `note` 字段（字符串，注释说明，仅作人工阅读，执行时忽略）。
- **未定义的字段一律忽略**（不报错、不校验）：结构校验仅针对已定义字段的必填性与类型，便于日后扩展。
- **数值 / 结构校验总则**（加载阶段整体执行一次）：①**结构非法**——根不是动作数组、动作缺必填字段、字段类型错误、`type` 未知、`pos` / `size` 数组长度不是 2 或元素非数字、`start` / `end` 时间格式非法——一律视为**加载失败**，整体拒绝、不绑定；②**数值越界**——`opacity` 超出 0~100、`volume` 为负、`ttl` 为负——按最近边界**截断（clamp）**，不视为非法。

#### 8.1 各类动作的结构

**（1）text —— 文本输入**

```json
{ "type": "text", "value": "第一行{ENTER}第二行{BS 9}" }
```

- `value`（必填，**字符串 或 字符串数组**）：写入的文本，从**执行动作那一刻的当前焦点窗口**光标处开始。
  - **字符串形态**：`\n` 即换行（单行写法，适合短文本）。
  - **数组形态**：每个元素代表一行，执行时按序拼接并在行间插入换行（适合大段多行文本，避免在一行里堆 `\n`）；数组元素必须都是字符串。

```json
{ "type": "text", "value": ["第一行", "第二行{ENTER}", "第三行"] }
```

- `value` 中形如 `{...}` 的部分按 AHK 键名解释（如 `{ENTER}` 回车、`{BS 9}` 退 9 格），**其余字符（含 `+` `!` `#` `^` `%` 等修饰符）一律按字面原样输出**，不得被 `Send` 解释为修饰键；字面 `{` / `}` 用 `` ` `` 前缀转义（`` `{ `` / `` `} ``）。此规则对字符串与数组的每个元素均适用。

- `delay`（可选，**毫秒**）：字符输出间隔，控制打字速度。缺省 `0` = 即时输出（保持原有行为）；`0` 表示无间隔，正数表示每发出一个字符（或按键动作）后等待 `delay` 毫秒。负值按最近边界截断为 `0`。此字段是**整条 text 动作**的统一间隔，数组的每一行共用。

- **`{Delay N}` 文本内段级延迟记号**（自造扩展，非官方）：在 `value` 的文本流中插入 `{Delay 100}`，可对**同一段文本内部**进行分段控制——从该记号起，**后续所有字符与按键动作**均按 `N` 毫秒间隔输出，直到遇到下一个 `{Delay M}` 切换；记号本身**不输出**。`N` 为非负整数，负值截断为 `0`。
  - 优先关系：`delay` 字段作为整串的**初始**间隔；`{Delay N}` 在运行时**覆盖/切换**。
  - 典型用法——数组内"某些行快、某些行慢"：因 `delay` 是整条统一值，在需要慢速的那行**行首**放 `{Delay 100}`，其余行即时；要切回即时再加 `{Delay 0}`：
    ```json
    { "type": "text", "value": [
        "==================================================",
        "{Delay 100}【play 模式 · 使用教程】本脚本就是教程",
        "================================================={ENTER}"
    ] }
    ```
    也支持同段内部前半即时、后半慢：`"{Delay 0}前半{Delay 100}后半"`（`{Delay 0}` 前无内容时仅确定初始终延迟）。

**（2）paste —— 贴图**

```json
{ "type": "paste", "path": "images/fig1.png", "pos": [100, 200], "size": [400, 300], "opacity": 80 }
```

- `path`（必填）：图片文件路径。
- `pos`（可选 `[x, y]`）：贴图窗口左上角屏幕坐标；缺省水平竖直居中（主显示器）。支持**负值居中语义**：`x < 0`（且 `y >= 0`）→ **水平居中**、竖直位置由 `y` 确定；`y < 0`（且 `x >= 0`）→ **竖直居中**、水平位置由 `x` 确定；`x < 0` 且 `y < 0` → **水平竖直都居中**（等效于未设置 `pos`）。
- `size`（可选 `[w, h]`）：显示尺寸；缺省 = 原图尺寸，指定后拉伸到该精确尺寸（不保持宽高比）。
- `opacity`（可选 0~100）：不透明度；缺省 100，越界按最近边界截断。
- `delay`（可选，**毫秒**）：**延时贴图**时长。缺省 `0` = 贴图动作执行后**立即贴图**；> `0` = 贴图动作执行后经 `delay` 毫秒**才实际贴图**（延迟期间该动作占用执行中标记，脚本不推进）。
- `ttl`（可选，**毫秒**）：**实际贴图后**的延续时长——贴图窗口显示 `ttl` 毫秒后自动销毁。缺省 `0` = **不自动销毁**，由用户自主销毁（保持原有行为）；> `0` = 实际贴图成功后，经过 `ttl` 毫秒自动销毁该贴图窗口。
- `wait`（可选，布尔）：是否等待贴图窗口关闭后才继续。**语义规则**：`delay = 0` **且** `ttl = 0` **同时成立**时 `wait` **无意义**（无论设置为何值都被忽略，始终相当于 `false`）；只要 `delay > 0` **或** `ttl > 0`，`wait` 就**有意义**，缺省 `false`。`wait` 有意义且为 `true` → 该 `paste` 动作在贴图窗口被关闭（`ttl` 到期自动销毁，或用户手动销毁）后才完成，脚本继续；`wait` 无意义或为 `false` → 贴图后立即完成，无须等待。
- `pin`（可选，布尔，缺省 `false`）：**置顶守护**——该贴图在其存活期间始终保持在**本次实际贴图那一刻已存在的全部贴图**之上（如先贴 A 再贴 `pin: true` 的 B，则此后无论用户是否用鼠标点击 A，B 都保持压在 A 上面）。实现：实际贴图成功后，把该贴图窗口句柄与"贴图时已存在的贴图句柄集合"登记进守护列表，由约 100ms 的轮询定时器沿 Z 序链（`GetWindow` + `GW_HWNDPREV`）检查——一旦发现任一登记的被压住窗口跑到该贴图窗口上方（典型场景：用户点击了被压住的旧贴图，Snipaste 将其置前），就用 `WinMoveTop`（不抢焦点、不改位置尺寸）把该贴图提回最前；该贴图销毁（`ttl` 到期自动销毁或用户手动销毁）后自动解除守护，被压住窗口销毁后也不再参与检查。语义要点：①被压住的旧贴图无法再靠点击保持置前（点击后会被守护立即盖回），销毁旧贴图建议使用 `ttl` 或另行方式；②后贴的新贴图不受该守护影响，仍按"新贴图在前"自然叠放（新贴图不在守护的"被压住"集合内）。
- 实现：按 `size` / `opacity` 需要时先生成临时 PNG（缩放与透明度写入图片本身），再通过 Snipaste `paste --files` 贴图；若可执行文件路径或图片路径含空格，则复制到无空格临时目录后再贴图。贴出后再移动到 `pos`（缺省居中）。`delay > 0` 时用一次性定时器在 `delay` 毫秒后执行实际贴图。`ttl > 0` 时用定时器在 `ttl` 毫秒后自动销毁该贴图窗口——销毁方式：短暂激活该贴图窗口并发送 `Esc`（与 Snipaste 官方按 Esc 销毁贴图的交互一致），销毁后恢复原焦点；若 `Esc` 未生效（极端情况）再补发 `WM_CLOSE` / `WM_SYSCOMMAND+SC_CLOSE` 尽力而为。`delay > 0 || ttl > 0` 且 `wait = true` 时，贴图动作以轮询方式等待该贴图窗口关闭后才完成（`wait` 语义见上）。

**（3）audio —— 播放音频**

```json
{ "type": "audio", "path": "audio/bgm.mp3", "start": "00:10", "end": "00:30", "volume": 0.8, "wait": true }
```

- `path`（必填）：音频文件路径。
- `start` / `end`（可选）：起止时间（`HH:MM:SS` 或秒数）；缺省 0 / 播到结尾。
- `volume`（可选）：音量增益倍数；缺省 1.0，负值截断为 0。
- `wait`（可选，布尔）：是否等待播放结束；缺省 `false`（启动即完成，后台继续播）。
- 实现：`ffplay -nodisp` + `-ss` / `-t`（`-t = end − start`）/ `-af volume=...`。`wait = true` 时**非阻塞**等待——启动后立即返回，跟踪 ffplay 进程退出判定完成，进程退出即视为完成（不深究退出原因）。`path` 不存在由 `FileExist` 拦截判执行失败。

**（4）video —— 播放视频**

```json
{ "type": "video", "path": "video/demo.mp4", "pos": [300, 200], "size": [800, 450], "opacity": 100, "wait": true }
```

- `path`（必填）：视频文件路径。
- `pos` / `size`（可选）：播放窗口位置 / 尺寸；缺省居中 / 原始尺寸，指定尺寸后画面在窗口内**等比缩放**（保持宽高比）。`pos` 与 `paste` 一致支持**负值居中语义**：`x < 0`（且 `y >= 0`）→ **水平居中**、竖直位置由 `y` 确定；`y < 0`（且 `x >= 0`）→ **竖直居中**、水平位置由 `x` 确定；`x < 0` 且 `y < 0` → **水平竖直都居中**（等效于未设置 `pos`）。
- `opacity`（可选 0~100）：缺省 100，越界截断。
- `wait`（可选，布尔）：缺省 `false`（启动即完成）。
- `start` / `end` / `volume`（可选）：与 `audio` 完全一致（起止时间、音量增益）。
- 实现：`ffplay` + `-left` / `-top` / `-x` / `-y` 指定窗口位置尺寸（`pos` 含负值时 `-left`/`-top` 不传，启动后读取窗口实际尺寸用 `WinMove` 做居中修正），`WinSetTransparent` 设透明度；`wait = true` 时**非阻塞**等待——跟踪 ffplay 进程退出或播放窗口关闭判定完成。`path` 不存在判执行失败。

**（5）seq —— 顺序嵌套动作**

```json
{ "type": "seq", "step": true, "actions": [ <动作>, <动作>, ... ] }
```

- `actions`（必填，动作数组）：子动作列表；空数组合法但立即完成。
- `step`（可选，布尔）：**单步 / 一次性**。`true`（缺省）= 单步——一次步进命令只执行第 1 个子动作；`false` = 一次性——一次步进命令依次执行完所有子动作。
- 子动作**始终按次序串行**（前一个完成再启动下一个）；`step` 只控制一次推进多少个子动作。
- **嵌套规则**：`seq` 的子动作按次序串行执行；当某个嵌套 `seq` 真的被启动时，仍按它自身的 `step` / `false` 配置决定是单步还是一次性。

**（6）par —— 并行嵌套动作**

```json
{ "type": "par", "actions": [ <动作>, <动作>, ... ] }
```

- `actions`（必填，动作数组）：子动作列表；空数组合法但立即完成。
- **并行启动**所有子动作，全部完成后该 par 才算完成；par **无单步**。
- **嵌套规则**：`par` 只负责并行启动其直接子动作；其中若包含 `seq` 子动作，则该 `seq` 在被启动后仍按它自身的 `step` / `false` 配置决定行为。

#### 8.2 步进（step）执行语义

- 脚本根是 seq（顶层动作数组）。维护两个状态：**步进游标栈**（栈底为顶层游标，指向下一个待执行顶层动作；进入单步 seq 压入其内部游标，该 seq 子动作全部执行完再弹出）与**执行中标记**（当前是否有动作在执行、尚未完成）。初始状态：游标栈仅含指向第 0 个顶层动作的顶层游标，执行中标记为假。
- **绑定即执行第 1 个动作**（无需再按步进命令）；顶层数组为空则绑定后立即自动解绑。
- 每次触发步进命令（Ctrl+R）：
  - 执行中标记为真 → 本次无动作；
  - 否则启动栈顶游标指向的动作：`text` / `paste` 立即执行（期间标记为真，完成前移游标）；`audio` / `video` 依 `wait`（`false` 启动即完成，`true` 非阻塞等待播放结束，期间标记为真）；`seq` 单步（`step=true`）只启动第 1 个子动作并压入内部游标（seq 自身不占标记，子动作全部完成才弹出、顶层游标前移）；`seq` 一次性 / `par` 依次 / 并行执行完所有直接子动作（期间标记为真）。
- 顶层游标越过最后一个动作 → 执行完毕、自动解绑、回到关闭状态。失败处理同第 7 节。

> 注：`audio` / `video` 是否阻塞仅由 `wait` 决定（与是否嵌套无关），`wait=false` 后台继续播放、不阻塞步进；"播完提示音再打字"可把两者按序放入同一 seq 并给提示音设 `wait=true`。解绑不影响已在后台播放的 `audio` / `video`（继续播完）。

#### 8.3 辅助动作类型（sleep / run / note）

除 8.1 的 6 类基础动作外，`type` 还支持 3 类辅助动作，用于编排演示流程：

```json
{ "type": "sleep", "duration": 1.5 }                        "duration"（必填，秒/时间，HH:MM:SS 或秒数）：等待后继续
{ "type": "run", "path": "C:/tools/x.exe", "args": "-a", "hide": false, "wait": false }
{ "type": "note", "text": "提示文字", "duration": 2.5 }
```

- `sleep`：`duration`（必填，秒数或 `HH:MM:SS`）——睡眠指定时长后继续下一步；非负，负值视为加载失败。
- `run`：`path`（必填，可执行文件 / URL / mailto）——运行外部程序；`args`（可选，参数字符串）、`hide`（可选，是否隐藏窗口）、`wait`（可选，是否等其退出）。`path` 不存在启动失败时记日志 / 提示后跳过（同第 7 节失败处理），不中断脚本。
- `note`：`text`（必填，注释提示文字）+ `duration`（可选，默认 2.5s）——以气泡提示显示文字，到点后继续下一步。
- 上述 3 类的**未定义字段一律忽略，结构 / 数值校验规则与 8.2 一致**（缺必填、类型错、`duration` 非法 → 加载失败整体拒绝；数值越界按最近边界截断）。

#### 8.4 完整示例

```json
[
  { "type": "text", "value": "大家好，欢迎来到演示{ENTER}" },
  { "type": "paste", "path": "images/title.png", "pos": [100, 100], "size": [600, 200] },
  {
    "type": "seq",
    "step": false,
    "note": "先放提示音，提示音结束后自动打字（step=false：一次步进依次执行完；audio 设 wait=true 等播完再打字）",
    "actions": [
      { "type": "audio", "path": "audio/ding.mp3", "wait": true },
      { "type": "text", "value": "（提示音结束）开始讲解" }
    ]
  },
  {
    "type": "par",
    "note": "背景音乐与演示视频同时播放",
    "actions": [
      { "type": "audio", "path": "audio/bgm.mp3", "volume": 0.5 },
      { "type": "video", "path": "video/demo.mp4", "pos": [400, 200], "size": [800, 450] }
    ]
  },
  {
    "type": "seq",
    "step": false,
    "note": "外层一次性 seq：会一次性推进它自己的直接子动作；嵌套子 seq 是否单步仍看该子 seq 自身的 step",
    "actions": [
      {
        "type": "seq",
        "step": true,
        "note": "此处的 step=true 被外层一次性 seq 覆盖，实际按一次性执行",
        "actions": [
          { "type": "text", "value": "第 1 段" },
          { "type": "text", "value": "第 2 段" }
        ]
      },
      { "type": "text", "value": "第 3 段" }
    ]
  }
]
```

> 说明：JSON 键名（`type` / `note` / `path` / `value` / `actions` / `step` / `wait` / `pos` / `size` / `opacity` / `pin` / `start` / `end` / `volume`）采用英文（与 `config.ini` / `config.ini.example` 一致）；动作内容（如 `value` 与示例中的中文）可自由使用中文。示例中的 seq 显式写 `"step": false` 表示一次性依次执行完；省略 `step` 或写 `"step": true` 则为单步（默认）——一次步进只执行该 seq 的第一个子动作。`audio` / `video` 省略 `wait` 或写 `"wait": false` 表示启动即算完成，写 `"wait": true` 表示等待播放结束。最后一个动作演示的是：**外层一次性 seq 会一次性推进它自己的直接子动作，而嵌套子 seq 是否单步仍取决于该子 seq 自身的 `step`**。

### 9. AI 模型配置

> 仅对 AI 模式有效

- 1）默认采用官方 Deepseek v4 flash  模型  （可以通过配置修改）
- 2）非上下文的 、对结果起约束作用的 提示语必须是可配置的，但是默认配置必须满足 前面的关于  AI 返回结果的要求。
- 3）接口风格可通过配置选择：默认采用聊天补全接口（chat，DeepSeek 官方默认、OpenAI 兼容）；如所用服务支持，可切换为原生补全接口（completion）。

### 10. 其它要求

- 所有的代码注释 和 程序运行中显示的文字 还有提示语 全部采用中文。
- 在配置中新增调试日志的控制开关。代码缺省值为不输出；仓库提交的 `config.ini.example` 当前示例值为输出，便于排查与复现。

### 11. 循环提醒（健康提醒）

> 循环执行若干阶段（默认“站立→坐下→走动”），直到按热键停止；阶段数与时长、名称、提示音皆可配置。

- 1）设计一个 **与所有默认快捷键无冲突** 的快捷键（默认 `Ctrl+Alt+H`，可在 `config.ini` 的 `[health] hotkey` 修改），用于**启动 / 停止**“循环提醒”（同一键切换）。
- 2）启动后循环执行 **站立 20 分钟 → 坐下 8 分钟 → 走动 2 分钟 → 回到站立** ……，直到再次按下该快捷键停止。各阶段时长由 `config.ini` 的 `[health] step_min` **列表**配置（默认 `[20, 8, 2]`，可用小数如 `0.1`=6 秒便于快速体验）。
- 3）每个阶段切换时：
  - ① 必须有**声音提醒**（各阶段音调组合不同，便于听声辨认；可在 `[health] sound` 关闭）；
  - ② 屏幕**右上角**显示**显著提示文字**：默认“站立20分钟 / 坐下8分钟 / 走动2分钟”（文字随 `[health] step_name` 列表配置），并**5 秒后自动消失**（时长可在 `[health] notify_ms` 修改，默认 5000 毫秒）。
- 4）托盘菜单中必须显示“循环提醒”的**当前状态**：停止 / 站立20分钟 / 坐下8分钟 / 走动2分钟（文字随 `step_name` 配置），并随启动、停止、阶段切换**实时刷新**（该托盘项点击亦可启动/停止，与热键等效）。
- 5）各阶段的提示声音**可用中文女声语音代替**：`config.ini` 的 `[health] step_sound` **列表**配置各阶段音频文件路径（WAV/MP3，相对路径以脚本目录为基准）时，对应阶段切换播放该音频；仓库自带三段女声 WAV（`audio\20-8-2-stand.wav` / `audio\20-8-2-sit.wav` / `audio\20-8-2-walk.wav`）。**未配置（留空或被注释掉）、路径无效或播放失败时，回退使用默认蜂鸣提示**（站立=上行三音、坐下=下行三音、走动=高低两音）。
- 6）阶段参数用 **等长列表** 表达，三行**下标一一对应**（`step_name` / `step_min` / `step_sound`），列表语法为方括号包裹 + 英文逗号分隔（如 `step_min = [20, 8, 2]`）；`step_min` 必填，`step_name` / `step_sound` 可缺省——缺省 `step_name` 时自动按“站立N分钟 / 坐下N分钟 / 走动N分钟”命名（或直接使用 `step_name` 提供的任意自定义文字），缺省 `step_sound` 时全部阶段用默认蜂鸣；支持 3 个及以上的任意阶段数（循环自动按阶段数闭环）。

### 12. 径向菜单（Radial Menu）

> 独立于 latex / unicode / AI / tikz / play 五个模式，拥有独立的全局快捷键入口。触发后弹出一个**圆形三层菜单**，通过点击扇区导航，最终执行预配置的快捷键。**圆盘的外观形式（圆形窗口、单环扇区、放射性文字、配色与尺寸自适应）保持不变，仅调整执行流程与各层展现的内容。**

- 1）配置位于 `config.ini` 的 `[radial]` 段。`trigger` 为触发快捷键（默认 `Ctrl+Shift+M`）；各功能组以 `[radial.xxx]` 子节定义（`xxx` 为英文标识符，不显示），组的显示名由 `name` 值指定（中文）；组内功能以编号 `1`/`2`/`3`... 为 key，值格式为 `中文功能名 | 动作`，编号决定菜单排列顺序。动作支持两种写法：①旧写法 `快捷键`；②新写法 `run: 完整命令行`，用于启动指定路径并可附带参数的程序。组的出现顺序等于节在文件中的行序。
- 2）触发键为**开/关切换**：菜单未打开时触发则弹出菜单；菜单已经打开时再次触发则**关闭菜单**。
- 3）菜单为**三层**圆形布局，各层圆心与周边内容如下：
  - **第一层（圆心【常用】）**：周边第 1 个固定为**【快捷菜单】**，其后为**按统计结果从高频到低频取前 N** 的常用菜单项（N 由 `[radial] common_max` 配置，默认 **6**，取值 0~20）。点【快捷菜单】进入第二层；点某常用项**只执行其动作（可以是快捷键，也可以是 `run:` 启动程序）且不关闭菜单**；点圆心【常用】**关闭菜单**。
  - **第二层（圆心【快捷菜单】）**：周边为**各功能分组名**。点某分组进入第三层；点圆心【快捷菜单】**返回第一层**。
  - **第三层（圆心【<分组名>】）**：周边为该组内**各菜单项**。点某菜单项**只执行其动作（可以是快捷键，也可以是 `run:` 启动程序）且不关闭菜单**；点圆心【<分组名>】**返回第二层**。
- 3b）执行功能后菜单**保持打开**，以便连续执行多个功能；关闭菜单只能通过触发键、Esc、鼠标右键或第一层圆心。
- 3c）**圆心区域**同时支持两种操作，二者需可区分：①**点击**——第二/三层返回上一层，第一层关闭菜单；②**按住拖拽**——移动整个圆盘的位置。判定方式：按住后位移超过阈值即视为拖拽，未超过则松开时视为点击。
- 3d）**拖拽必须只移动圆盘**：不得触发圆心点击、不得切换层级、不得关闭菜单、不得改变焦点或前台窗口、不得产生任何其它可感知的连带动作。圆盘位置与鼠标位移 1:1 对应，拖动后的位置在切换层级时保持不变；圆盘位置须限制在**全部显示器合并区域**内（不得因多显示器而被拉回主显示器）。
- 4）每层周边**至少 4 个扇区**；实际菜单项不足 4 个时，多出的空位扇区**无文字、禁止高亮、点击无效**。
- 5）菜单以鼠标当前位置为圆心弹出。所有菜单项在**单个圆环**上按角度等分排列。扇区文字采用**放射性排布**（沿扇区径向、正立可读），且**文字视觉中心必须对齐对应按钮中心**，最多显示 **8 个汉字**，超出按实际渲染宽度截断并追加省略号。
- 6）字体大小可在 `[radial] font_size` 配置（缺省沿用全局 `[ui] font_size`）。圆盘尺寸（中心圆与外环半径）须随字号大小与各菜单名的实际长度**自动适配、尽可能紧凑**。
- 7）支持 Esc 键和鼠标右键关闭菜单。菜单窗口**不得抢占焦点**（点击 / 拖拽菜单均不改变前台窗口）。菜单与四个屏幕小键盘**互相独立、可同时显示**；五者共用 `Esc` 时，`Esc` 关闭的是**最近操作过的那个**浮层（打开、鼠标移入、点击——含点空位/无效点击——都算一次操作）。执行菜单项时，快捷键必须始终作用于**点击当下的当前前台窗口**；若该窗口内存在当前选择，则快捷键只作用于该窗口里的当前选择。
- 7b）执行菜单项对应快捷键前，必须先确认**点击当下的当前前台窗口**存在且可用；若当前前台窗口异常、丢失、或前台切换未成功，则**取消本次发送**，不得回退到菜单打开前的旧窗口，也不得把快捷键发往其它窗口。发送前还须等待 **Ctrl / Shift / Alt / Win** 等物理修饰键释放；若在合理超时内未释放，同样取消本次发送，以降低系统级快捷键误发风险。
- 8）若 `config.ini` 不存在或无 `[radial]` 段，则不注册触发快捷键，静默跳过。
- 9）须统计每个**菜单项**（配置项）的执行次数：每执行一次即 **+1** 并**实时写入**独立文件 `menu_stats.ini`（与脚本同目录）。该文件不存在时**自动创建**；某配置项此前未统计过则从 0 开始、首次记为 1。统计键须与 `config.ini` 的 `[radial.*]` 子节**一一对应**，形如 `radial.<组标识>.<编号>`（如 `radial.base.1` 对应 `[radial.base]` 下编号 1 的那一项），故修改快捷键/功能名不影响已累计次数；该统计即第一层【常用】高频项的来源。

- （2026-09-15 补充）径向菜单的菜单项动作必须与四块小键盘**共用同一套动作定义**，共用集合为：普通按键 / 文本、`run:` 启动程序、`close` 关闭本层浮层、`case` 切换本浮层大小写状态、`self:` 调用本软件自身功能（命令表与四块小键盘完全一致）。**不得**把 `self:` 之类的前缀当成普通文本发送出去。以后新增动作类型时，必须同时对小键盘与径向菜单生效（只在共用的动作层里扩展，不允许某个组件单独实现一套）。

### 13. 屏幕小键盘（方向 / 数字 / 符号 / 字母）

> 四个独立于各模式的全局快捷键入口，各自弹出一个屏幕按键面板，用于在数位板 / 触屏下用笔点按按键。这四块面板与 latex / unicode / AI / tikz / play 各模式互不干扰。

- 1）配置位于 `config.ini` 的 `[keypad]` 段。`arrow_hotkey` 为方向小键盘触发键（默认 `Ctrl+Shift+K`）；`numpad_hotkey` 为数字小键盘触发键（默认 `Ctrl+Shift+N`）；`symbol_hotkey` 为符号小键盘触发键（默认 `Ctrl+Shift+Y`）；`letter_hotkey` 为字母小键盘触发键（默认 `Ctrl+Shift+E`）。四者均可通过配置修改。
- 1b）四个面板的**按键内容必须由配置驱动**，且四个面板共用同一套实现（不得为某个面板写死专属逻辑）。每个面板一个独立子段（`[keypad.arrow]` / `[keypad.numpad]` / `[keypad.symbol]` / `[keypad.letter]`），段内以「编号 = 名称 | 动作」逐格定义：编号即格子序号（行优先，1 起），缺号 = 空位（不绘制、不可点）；行列数（`cols` / `rows`）与"按键是否正方形"（`square`）、"是否启用大小写状态"（`case`）均可配置。动作须支持：普通按键 / 文本、`run:` 启动程序、`close` 关闭本面板、`case` 切换大小写、`self:` 调用本软件自身功能（**不得**依赖"模拟按键再触发自身热键"）。某面板未写任何按键时沿用内置默认；配置缺失或取值非法时一律沿用内置默认并夹取到合法范围。示例配置须与内置默认完全等价。
- 2）触发键为**开 / 关切换**，且**各自只管自己的面板**：该面板未打开时触发则弹出；已打开时再按同一触发键则关闭。四块面板**互相独立**，按其中一个的触发键不得关闭、切换或影响另外三个。
- 2b）方向小键盘、数字小键盘、符号小键盘、字母小键盘与径向菜单**五者互相独立**，可以同时显示；打开或关闭任意一个都不得联动关闭另外几个。
- 2c）五者都不获得键盘焦点，其 `Esc` 关闭由同一套全局热键统一接管：只要有浮层打开就接管 `Esc`，按一次 `Esc` 关闭**最近操作过的那个**浮层；"操作"包含：打开该浮层、鼠标在其上移动（含拖动）、在其上点击（**含点空位 / 无效点击**）。全部浮层关闭后立即归还 `Esc`（不得影响其它程序）。
- 2e）**浮层上的所有文字（含其黑色描边）必须始终完全不透明**，且都必须画在不受 `opacity` 影响的文字层上：文字颜色可配置（`[ui] overlay_text_color`，默认亮黄 `FFFF00`），描边宽度可配置（`[ui] overlay_text_outline`，默认 1 像素，0 = 关闭）。`opacity` 参数只控制浮层本体（背景与按键底色 / 扇区底色），不得让文字一起变淡；文字的抗锯齿不得因此出现描边或色边。该规则对四块小键盘与径向菜单**同样适用**。
- 2d）**五块浮层同屏时不允许互相重叠**：以"正在拖动的那块"或"刚打开的那块"为准，被它压住的浮层必须自动让开——沿位移最小的方向推开，两两之间保留约 8 像素间隙；被推开的浮层若又压到第三块，须继续连锁让开。所有落点必须限制在全部显示器合并区域内；若某个方向已顶到屏幕边缘仍会重叠，应改从另一个方向让开；两个方向都无法避让时保留最小重叠，且不得出现来回抖动。
- 3）**方向小键盘的内置默认**采用 3×3 布局：四周为四个方向键与四个功能键——左上【退格】、右上【删除】、左下【上页】、右下【下页】；中心键为【回车】。点击四个方向键分别发送上、下、左、右；四个功能键分别发送退格、删除、Page Up、Page Down；中心键发送回车。
- 4）**数字小键盘的内置默认**采用 4 列 × 4 行排布（`7 8 9 +` / `4 5 6 -` / `1 2 3 ×` / `0 . ÷ 回车`）。数字、小数点与四则运算符发送普通字符（`+` `-` `*` `/`，不受 NumLock 状态影响），回车发送确认键。
- 4b）**符号小键盘的内置默认**采用 6 列 × 5 行、共 30 键的排布：标准键盘上**除方向面板与数字面板已有键之外**的全部符号（`` ` `` `~` `!` `@` `#` `$` `%` `^` `&` `(` `)` `_` `=` `[` `{` `]` `}` `\` `|` `;` `:` `'` `"` `,` `<` `>` `?`），外加一个重复的正斜杠 `/`、以及【空格】与【Tab】两个常用键（用于补满 30 格）。点击即发送对应的普通字符 / 键（不受 NumLock 状态影响）；除有意重复的正斜杠外，其余键不得与方向 / 数字面板重复。
- 4c）**字母小键盘的内置默认**采用 6 列 × 5 行、共 30 键的排布：`a`–`z` 按字母顺序排列，其后依次是**大小写切换键【Aa】**、**【回车】**（发送回车）、**反斜杠键**（发送 `\`）与**【触发】**。点击字母键发送对应字母；点击【Aa】键在**小写 / 大写**之间整体切换——大写状态下全部字母键的标签与发送内容均为大写，小写状态下均为小写。切换状态在本次运行期间保持；切换后面板须就地重建按键并重绘，且面板尺寸与位置不得变化。**【触发】键必须与 SharpKnife 自身的触发命令（默认 Ctrl+J）效果完全一致**，且不得依赖"模拟按键再触发脚本自身热键"这类不可靠做法。
- 5）点击按键后**面板保持打开**，以便连续点按多个按键；只有明确的关闭操作才关闭面板。
- 6）关闭方式：自己的触发键（再按一次）、`Esc` 键、鼠标右键（四套面板均不设置【关】键）。
- 7）面板以鼠标当前位置为中心弹出，位置须限制在**全部显示器合并区域**内；面板支持**按住拖动**移动位置（拖动只移动面板，不得触发任何按键）。判定方式：按住后位移超过阈值即视为拖拽，未超过则松开时视为点击；只有按下与抬起都落在同一按键上时才触发该按键。
- 8）面板窗口**不得抢占焦点**：点击按键、拖动面板均不得改变前台窗口、焦点与光标位置。点击按键时，该按键必须发送到**点击当下的当前前台窗口**；发送前须确认目标窗口可用并等待 `Ctrl / Shift / Alt / Win` 等物理修饰键释放，否则取消本次发送。
- 9）面板字体大小可在 `[keypad] font_size` 配置（缺省沿用全局 `[ui] font_size`），面板尺寸随字号与按键文字自动适配；`[keypad] opacity` 可配置面板透明度（0.0~1.0）。配置缺失或取值非法时沿用默认值。

### 14. 自然语言运行框（中文需求 → 模型解析成动作序列 → 确认后执行）

> 目标是"说一句中文就把事办了"：热键弹出输入框，用中文描述需求，交给 `[ai]` 里已配置的模型解析成一串动作，确认后自动执行。纯文字需求，不含实现细节。

- 1）配置位于 `config.ini` 的 `[runbox]` 段：触发键 `hotkey`（默认 `F6`）、`confirm`、`model`、`timeout_ms`、`step_delay_ms`、`run_wait_ms`、`max_actions`、`prompt_extra`。配置缺失或取值非法时一律沿用默认值。
- 2）按触发键弹出**单行输入框**（必须支持中文输入法），并且**光标自动落在输入框里**；回车提交、在运行框上按 `Esc` 取消并关闭运行框；**再次按触发键不得关闭运行框**，只把输入光标重新放回输入框（触发键只负责"打开 / 聚焦"）。弹出时必须**记住当时的前台窗口**，解析完成后所有会改变目标窗口的动作都必须作用在该窗口上；请求解析期间应把焦点还给该窗口，不得一直占着用户的编辑器。
- 3）解析使用 `[ai]` 里配置的模型与密钥（`model` 可单独覆盖）；请求超时沿用或覆盖 `[ai] timeout_ms`。
- 4）**判定顺序（用户明确要求）**：动作必须与四块小键盘 / 圆盘菜单使用同一套定义（共用动作层），不得为运行框单独发明一套。每条需求按下面的顺序判定：
  - **第一步、判类别**：这条需求是"**要输出文字**"（要网址、要答案、要一段内容、翻译、解释…），还是"**要执行某种操作**"（打开 / 启动程序、按键、切换、保存、删除、复制…要改变电脑状态）。
  - **要输出文字 → 直接走文字通道**：显式写 `send: 文本` / `hotkey: 文本` / `paste: 文本` 即为"要输出的文字"，**可以直接执行、不要求先在配置里建项**，且必须照用户要求的原话原样输出（模型不得改写、翻译或增删）。其中 `paste:` 是文本通道，任何符号都原样粘贴；`send:` / `hotkey:` 只在该文本**不含按键语法字符**（`^` `!` `+` `#` `{` `}`）时按纯文本放行。 **多行文字必须能完整输出**：约定写成**一行** `paste:`、行内换行写成字面 `\n`（程序还原成真换行）；同时把 `paste:` 行之后**紧跟的**、不属于已配置动作 / 名称、也不含按键语法的裸行，作为上一段文字的**续行**拼进同一条 `paste:`。两种情况都必须**一次粘贴、保留换行**，不得出现"只输出第一行（例如只粘出标题）"。
  - **要执行操作 → 走动作通道**，分两种情况：
    - ① **配置里有对应动作**：**必须与用户配置里某一项完全一致**才执行——即 `item: 名称`（按名称执行已配置项）、`run: 命令行`、`self: 命令`、按键 / 组合键原文；
    - ② **配置里没有对应动作**：程序必须让模型**回头重新审视这条需求**，判断它是否**也可以理解成"要输出文字"**——可以 → 走文字通道输出满足需求的内容；不可以（本质上就是要操作电脑）→ **只提示"没有对应的操作"，不得把任何文字打进用户的编辑器**。
- 5）解析前必须把用户的**「已配置命令表」**（四块小键盘每个按键 + 圆盘菜单每个菜单项的"名称 = 动作"）提供给模型，并要求其优先复用；模型**不得**使用表中不存在的 `run:` / `self:`。
- 6）**安全要求**：模型回复中除了上述动作行以外的任何内容（解释文字、未知前缀、空行以外的杂项）**一律不得执行**，只能作为"已丢弃"项展示给用户。**文字输出只有显式写成 `send:` / `paste:` 的行才允许**——没加前缀的裸文字（最常见的就是模型多写的解释）一律丢弃，不得作为普通文本原样发送；**唯一例外**是紧跟在 `paste:` 行之后、不属于已配置动作 / 名称、也不含按键语法的裸行，它作为上一段文字的续行（见 4）。模型判断做不了时，须支持只输出一行 `ERROR: 原因`（操作类且表里没有对应动作时输出 `ERROR: NOACTION`），此时不得执行任何动作。**兜底（4 的第二步②）那一次请求只允许文字输出**：程序只接受文字（`paste:`；模型漏写前缀的纯文本也会按粘贴处理），按键 / `item:` / `run:` / `self:` 等动作一律丢弃，保证兜底路径不可能执行动作。
- 6b）**运行框执行的每一个"动作"都必须是配置（圆盘菜单 + 四块小键盘）里已经存在的动作**：属于"要执行操作"的需求，必须被解释成"由这些已配置动作组成的一个动作串"，再依次执行。**不得**执行配置里没有的按键 / 快捷键 / 程序 / 功能名；凡是不在配置里的动作行一律丢弃，并在"执行过程"里列出丢弃原因。**输出文字不受此限**：属于"要输出文字"的需求（含配置里没有对应动作、但回头审视后判定可以按文字输出的需求）允许模型自由给出文字，由 `send:` / `paste:` 直接输出（`send:` 仍不得夹带按键语法，见 4）。匹配动作时必须**区分大小写**（`item: A` 与 `item: a` 是不同项，分别执行配置里的 A / a；不得因为大小写不敏感而把小写项当成大写项执行）；对 `case = true` 的小键盘（字母键盘），动作表须同时提供小写与大写两种字母，保证"输出大写字母"这类需求可以正确完成。
- 7）**执行前确认**：`confirm = true`（默认）时，必须在状态行给出"将执行 N 条 + 已丢弃 M 行"的**摘要**，用户回车确认后才执行、`Esc` 取消；`confirm = false` 时可直接执行。
- 7b）**界面只允许两块**：① 输入框；② 可展开 / 收起、可关闭的"动作执行过程"面板。**不得**再设独立的"清单展示框"之类的第三方区域；清单明细与丢弃原因都属于"执行过程"的内容，只有展开该面板时才显示（收起时不显示）。
- 8）执行时逐条按顺序执行，动作之间按 `step_delay_ms` 间隔、`run:` 之后额外等待 `run_wait_ms`；等待由程序自动完成，不得要求模型输出等待动作。执行过程中须显示当前进度，并且**按 `Esc` 可以立即中止剩余动作**。
- 8b）**执行完毕（或因 `Esc` 中止）后必须回到可编辑状态**：输入框清空（焦点归属见 8f），用户不必再按触发键即可直接输入下一条需求；刚执行过的动作清单应保留可见以便回看；运行框仍保持打开，直到用户在运行框上按 `Esc` 关闭（触发键不再关闭运行框）。
- 8e）**运行框底部必须有一个可点击的小把手，用于展开 / 收起"执行全过程"面板**：展开后须能看到本次的完整过程——原始需求、模型解析结果（解析出多少条、丢弃多少行）、将执行的每一条动作与被丢弃的每一行（含原因）、实际执行时每一条的成败与耗时、以及最终结果与总耗时。展开 / 收起不得影响输入框的继续输入。（2026-09-18 用户改定：执行过程为**独立的不透明只读多行文本框**，必须**可用滚动条翻阅、可用鼠标选中、可 `Ctrl+C` 复制**，展开时自动滚到底；完整过程同样逐行写入 `debug.log` 以便回看。）
- 8i）**自然语言运行框必须与圆盘菜单、四块小键盘互不覆盖**：采用与它们相同的避让机制——以"刚打开 / 正在拖动"的那一方为准，把被压住的浮层沿最小位移方向推开（保留间隙、夹取虚拟屏幕；推不动则原地不动）。运行框与其下方的热键键帽排必须视为**一个整体**参与避让、一起移动，不得被拆散；拖动运行框结束时同样要推开被压住的浮层。
- 8h）**`[radial]` 的每个分组必须支持"隐藏"参数**（如 `hidden = true`，缺省 = 不隐藏）：被隐藏的分组**不得出现在圆盘菜单中**——第二层【快捷菜单】里不列出该分组，第一层【常用】里也不得出现该分组的功能；参数值非法时按"不隐藏"处理。
- 8j）**两种"取消"必须区分开**：在运行框上按 `Esc` 是对**当前窗口**（即运行框自己）的取消 → **关闭运行框**；而点击运行框正下方键帽排的【取消】是对**"除运行框之外最近活动的那个窗口"**的取消 → 把 `Esc` 发给该**目标窗口**，运行框保持打开。键帽排上的**所有**键（回车 / Tab / 空格 / 删除 / 退格 / 取消 / 触发）都必须以该目标窗口为作用对象，不得作用于运行框自身。
- 8g）**运行框正下方必须有一排热键键帽**：依次为 回车 / Tab / 空格 / 删除 / 退格 / 取消 / 触发（`self:trigger`），横向依次排开，吸附在运行框正下方并随运行框一起移动（含展开 / 收起执行过程、拖动窗口）。点击键帽应当把对应按键发送给运行框跟踪到的目标窗口，键帽本身不得抢焦点。**键帽字符的款式（字体、字号、颜色、黑边）必须与小键盘、圆盘菜单上的字符完全一致**。键帽排必须可通过配置独立定制：在 `config.ini` 的 `[runbox.runkeys]` 子节里按「编号 = 名称 | 动作」（另可配 name / cols / rows / square / case）书写，缺省时沿用内置默认键帽。
- 8f）**执行完成后的焦点归属按"有没有打开新窗口"决定**：① 动作**打开了新窗口**（启动程序、打开 Windows 自带运行框等任何成为前台的新窗口）→ 焦点必须留在该新窗口内，不得把焦点抢回运行框，也不得还给执行前的旧窗口；② **没有打开新窗口** → 焦点回到执行前那个"最近一次活动的窗口"（即动作的目标窗口，例如用户原来在编辑的文本框）；③ 判定"是不是新窗口""上一个窗口是谁"时，**必须把自然语言运行框自己（含其下方的热键键帽排）排除在外**：它可以在需要时持有焦点（用户在里面打字），但永远不得作为动作的目标窗口，也不得作为"要回退到的上一个窗口"；④ 若没有可用目标窗口，则退回聚焦运行框。运行框应继续留在屏幕上且不抢焦点；"点输入框"或"再按一次触发键"都应当把焦点拿回运行框（触发键**永远只把光标拿回运行框、不关闭窗口**；关闭运行框只能通过在运行框上按 `Esc`）。
- 8d）**运行框必须可以用鼠标拖动移动位置**：按住面板上**除输入框之外**的任意位置（提示行 / 状态行 / 把手 / 面板空白处）即可拖动，且拖动不得影响输入框的正常输入（按在输入框上仍是定位光标与输入法），拖动时也不得被系统"双击最大化"。
- 8k）**运行框的透明度与文字描边（2026-09-18 用户明确要求）**：运行框除**输入框本身**之外的部分，必须与小键盘 / 圆盘菜单 / 键帽排等效——① 面板本体（底色与边框）受 `[runbox] opacity`（0.0~1.0，缺省 1 = 不透明）控制；② 面板上的全部文字（提示行 / 状态行 / 把手）都必须带**黑色描边**，并且**字身与黑边都恒不透明、不受 `opacity` 影响**（黑边宽度沿用 `[ui] overlay_text_outline`）；③ **输入框（白底黑字）与执行过程文本框都必须始终完全不透明**，`opacity` 对它们无效；④ 运行框正下方那排热键键帽的透明度与运行框一致（同样取 `[runbox] opacity`，不再取 `[keypad] opacity`）。
- 8c）**目标窗口必须是"最近一个活动的窗口"，且不得是运行框自己**：运行框打开期间需持续跟踪前台窗口的变化（用户中途切到别的程序时也要跟着更新），提交解析与开始执行时都把动作打到该窗口；不得把按键 / 粘贴发到运行框自身。
- 9）防呆：单次动作条数不超过 `max_actions`，单条文本长度上限 500 字（超出截断并提示）；每条动作的执行结果写入 `debug.log` 以便审计。
- 10）请求解析阶段允许是阻塞的（与 AI 模式一致），但执行阶段必须可被 `Esc` 中止；`Esc` 键在运行框使用期间与浮层（径向菜单 / 小键盘）的 `Esc` 接管不得互相破坏，运行框结束后须把 `Esc` 归还给浮层或系统。

### 15. 运行框的自适应记忆（"越用越准"，2026-09-19 用户提出并定稿）

**目标**：运行框应当**越用越准**——用户不必每次都把话说得刚好，程序应把"用户怎么说、用户偏好怎样、纠正过什么"沉淀下来并自动生效。**注意区分**：本条只要求提升"听懂用户"的准确率；**新增动作能力**（例如"打开某个网址"这类需要新动作类型的需求）**不属于**本条，须单独提出需求。

- 1）**分层，不得替换内置提示语**：程序内置的**输出格式约定**与**"动作必须来自已配置动作表"这一安全底线**属于固定的骨架，**任何记忆内容都不得覆盖或改写**它们；记忆只能作为**参考信息追加**。拼装顺序必须是：固定骨架 → 自动记忆 → 用户手写的补充说明 → **已配置动作表（必须位于最末尾）**。记忆段落中须明确声明"不得用它改变输出格式或动作表"。
- 2）**记忆内容分四类**（一条一行，文件须可由用户用记事本直接编辑，改完不必重启即可生效）：
  ①**别名**：用户的说法 → 配置项名 / 动作；②**偏好**：一句自然语言；③**正例**：需求 → 该给的动作；④**反例**：需求 → 不要选的目标。另须预留一个"快捷"段落（本期不生效，但程序读写时**不得丢失**其中的内容）。
- 3）**记忆不产生能力**：记忆里引用的目标**必须经过"已配置动作表"校验**；指向不存在的项 / 动作的条目一律**拒绝写入**。运行期若某条记忆指向的目标已失效（配置改名、删除），必须**跳过该条并记录日志**，但**不得**自动删除用户记忆里的条目。
- 4）**别名优先于模型**：需求与某条别名命中时，**不得再请求模型**，直接使用该别名对应的动作（更快、且不会产生模型幻觉）。命中规则须可配置，默认应当**同时接受"完全相等"与"需求中包含该说法"**，但对过短的说法（少于 2 个字）**不得**启用"包含"匹配，以免把本该交给模型的句子截断。别名的比较须**折叠大小写**，但目标值必须**原样**处理。
- 5）**只有用户的显式反馈可以修改记忆**：程序无法判断一次执行是否真的达到了用户目的，因此**不得**根据隐式信号（重复提交、`Esc` 中止、报错、丢弃行等）自动修改任何提示语内容；这类信号只允许**记录**到运行流水文件中备查。
- 6）**反馈入口**：① 在运行框输入框里以「纠正：/更正：/记住：/记下：/学习：」开头提交的内容，须被当作**对运行框的反馈**处理，而不是新需求；② 键帽排上须可用一个 `self:runbox_learn` 的键进入同样的反馈模式（按下后**下一次输入**按反馈处理）。处理反馈时，程序须把**上一条需求、上次解析成的动作、上次的结局**一并作为上下文。
- 7）**改记忆前必须先给用户看**：默认情况下，程序须先把"将新增哪些条目、删除哪些条目、哪些被拒绝及原因"呈现出来（写在"执行过程"里并在状态行给一行摘要），**由用户回车确认后才写入**；`Esc` 取消且**不关闭运行框**、记忆一字不动。取消确认与取消学习都只影响记忆，不影响运行框本身的开关。
- 8）**写入安全**：写盘前必须**自动备份**原文件（只保留最近若干份），写入须是**原子**操作（失败时原文件保持不动）；程序写入时必须**保留文件里不认识的段落与用户手写的注释**，不得整文件重写覆盖。须提供**回滚**能力（回到上一份备份）。被写入的条目总量与拼进提示语的字数都须有**上限**，超出时按与当前需求的相关度裁剪，并在日志中说明裁剪情况。
- 9）**不得自动学习**：任何"程序自己决定学点什么"的行为都不在需求内；记忆的变更**只能**由用户显式反馈触发。
- 10）**运行流水**：每次需求（含失败、被中止、被拒绝）须记录一条可读的流水（需求原文、结果、丢弃行数、错误、是否由别名命中、本次用到的记忆规模），**只追加、不参与提示语**，供日后复盘与改进。
- 11）**准确率须可衡量**：须提供一套评测手段，用同一批真实说法在"开启记忆 / 关闭记忆"两种状态下分别运行，给出准确率对比（整体并区分动作类 / 文字类 / 拒绝类），以证明"确实变准了"而不是主观感觉。评测所用的提示语必须与运行框实际使用的**完全一致**。
- 12）**支持"按住修饰键"的多步序列**（2026-09-19 用户实测提出）：运行框执行动作序列时，**不得**因为"前一步注入了修饰键、当前修饰键处于按下状态"而取消后续动作。即"按住某修饰键连续操作、最后松开"这类写法（如 `{Ctrl down}` → 若干按键 → `{Ctrl up}`）必须能完整执行。
  该放宽**仅限运行框**：圆盘菜单与四块小键盘（被真实组合键唤起）仍须等待物理修饰键抬起后注入，其行为不得改变；`run:`（启动程序）动作也不在此列。
  同时须有兜底：一次执行**结束或中止**时，程序须自动抬起"序列未能松开"的修饰键，不得让用户遇到修饰键卡住。对于用户**刻意跨多次需求保持**修饰键的用法，不做自动松开。
  另注：动作仍须与「已配置动作表」**逐字一致**，本项不改变这一点（例如 `^{Space}` 命中配置项，而 `^Space` 不算）。
- 13）**文件与隐私**：记忆文件、运行流水、以及评测/自查用的提示语导出文件都含用户的个人说法与使用习惯，**不得**提交进代码仓库。记忆功能必须可以**完全关闭**（关闭后行为与引入该功能之前完全一致）。
