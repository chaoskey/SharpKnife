> 一个基于 AutoHotkey v2 的全局 **LaTeX 命令 / Unicode 符号** 快速补全工具。在任何文本编辑框中按下 `Ctrl+J`，即可把光标前刚输入的 LaTeX 键补全为完整的 LaTeX 命令、Unicode 字符或环境模板。默认情况下所有数据来自本地触发表 `latexs.cvs`，**不联网、不需要任何 API**。当然如果切换到 AI 模式，还是需要的哦！
> 
> 本文附件有 latexs.cvs ， 但不提供实际代码，因为原则上用本文作为 AI 提示语，至少用 DeepSeek V4 Flash 可以生成代码。就算没能一次性测试通过，我相信通过简单几轮人工交互，最终也能生成测试通过的代码（事实上我就是这样做的）。

### 1. 验证环境：

Win10 +  **AutoHotkey v2**（[https://www.autohotkey.com/](https://www.autohotkey.com/) ，必须是 v2，不是 v1）

如果期望启用 tikz 模式，那么还需要安装： miktex + ghostscript + snipaste  

### 2. 整体逻辑

- *触发命令*  》  *上下文选择* (非法上下文则直接返回，无操作)  》  *上下文匹配*  （没有匹配上则直接返回，无操作） 》 *动作触发* （如果取消则直接返回，无操作）

- *循环切换命令* 》 *模式切换*   （latex（默认） → unicode → AI → tikz → latex）

- *直接切换命令* 》 *模式切换*   （直接切换到指定模式）

- *触发模式列表* 》 弹出无边框列表 》 *通过上下键移动选择进行模式切换*   （直接切换到指定模式）

- *步进执行命令* 》 *执行一步*   （play 模式专属；无论处于哪个状态都有效，关闭状态下弹出脚本文件选择窗口，开启状态下执行脚本的下一步）

- *模式关系* ： latex / unicode / AI / tikz 四种模式**互相排它**，有且仅有其中一种当前有效，它们共享同一个 *触发命令*（Ctrl+J）； play 模式是**独立模式**，可与上述四种模式之一**并存**，拥有独立的 *步进执行命令*（Ctrl+R）。 play 模式没有独立的开启/关闭操作，其状态由**是否绑定脚本文件**自然决定：已绑定脚本文件即处于开启状态，脚本执行完毕自动解绑即回到关闭状态。

### 3. 命令

- 触发命令 ：  Ctrl+J  （可通过配置修改； latex / unicode / AI / tikz 四种互斥模式共享）

- 循环切换命令 ： Ctrl+Shift+J   （可通过配置修改；仅在四种互斥模式之间循环）

- 直接切换命令 ： Ctrl+Shift+<数字>   （<数字>之前的前缀可通过配置修改， 0=latex，1=unicode，2=AI，3=tikz）

- 触发模式列表 ： Ctrl+Shift+\   （可通过配置修改）

- 步进执行命令 ： Ctrl+R   （可通过配置修改；play 模式专属，无论处于哪个状态都有效）

### 4. 模式切换

触发模式 ： 

- 循环切换 :  **latex 模式** （默认模式）； **unicode 模式**  ；**AI 模式**  ；**tikz 模式**  。 这四种模式**互相排它**，有且仅有其中一种当前有效； *切换命令*  在这四个模式之间循环切换。

- 直接切换 :  数字映射 0=latex，1=unicode，2=AI，3=tikz

- 触发模式列表选择切换 : 完整列表 latex 模式（0），unicode 模式（1），AI 模式（2），tikz 模式（3）

- play 模式 :  **独立模式**，不与上述四种互斥模式共用切换命令；它可与当前生效的互斥模式**并存**，拥有独立的 *步进执行命令*（Ctrl+R）。 play 模式没有独立的开启/关闭操作，其状态由**是否绑定脚本文件**自然决定：已绑定脚本文件即处于开启状态，脚本执行完毕自动解绑即回到关闭状态。

### 5. 上下文选择

触发后的上下文选择 ： 文字光标前的非空连续字符串 。

也可以在触发前人工选择指定内容作为上下文，然触发后直接用选择的上下文进行后续的匹配。

### 6. 上下文匹配

如果是 AI 模式 和 tikz 模式 ，那么不存在上下文匹配，任何非空上下文都都是合法的。并且直接用这个非空上下文作为 AI 提示语。

play 模式 不参与上下文选择与上下文匹配（步进执行完全依赖自定义格式脚本，与文本上下文无关）。

下面的 上下文匹配 仅仅针对 latex 模式  和  unicode 模式。

基于 触发表 ： latexs.cvs 的查表匹配。此表中也有详细注释。

unicode 模式 的触发表过滤 ： 第二个字段的字符串中间不能含 空格。

latex 模式 的 触发表过滤 ：第二个字段的字符串不能以 ：开头。

合法上下文： `<前缀><待匹配字符串>`  。其中`<前缀>`  可能是  `_`   ,  `^`  , `_\`   ,  `^\`  ,  `\` ；  `<待匹配字符串>` 代表   非空的  英文字母 或 数字 或 非 _ ^ \ 的符号 组成的区分大小写的字符串。   

对非法上下文触发的结果就是： 直接返回，无操作 。

合法上下文基于触发表第一个字段的匹配模式  ：`<前缀>*<待匹配字符串>*`  。   其中   `*`  代表  非空的  英文字母 或 数字 或 非 _ ^ \ 的符号 组成的区分大小写的字符串 或 空字符串。

上下文匹配结果只有四种可能类型：1）精确匹配  =（两个  `*`  都是空字符串）；2）头对齐匹配 `<`（第一个  `*`  是空字符串，第二个  `*`  是非空字符串）；3）尾对齐匹配 `>`（第一个  `*`  是非空字符串，第二个  `*`  是空字符串）；4）中间匹配 `~`（两个  `*`  都是非空字符串）。

完整的匹配结果：1）如果只匹配出一项，那么直接 *动作触发* ；2）如果匹配出两项或更多，那么先弹出一个无框列表，这个列表最多显示10项，但通过上下键移动，可以看见所有匹配项，然后回车选择实际要触发的项进行  *动作触发*。 在选择触发之前，也可以按取消键，取消触发。

关于无框列表的的各项的内容格式：<匹配类型> <第一个字段字符串> <第二字段字符串> 。 其中 <匹配类型> 形如： = > < ~ ； <第二字段字符串> 要剔除 : 前缀。

### 7. 动作触发

latex 模式的触发有两种情况：  
- 1） 只有两个字段时，触发结果就是： 用第一个字段的字符串 替换 上下文字符串，并且尾部补一个空格。  

- 2）有三个字段时，触发结果就是：用第三个字段解析后的字符串 替换 上下文字符串，并且将光标左移指定格数。

unicode 模式的触发结果：  用第二个字段的字符串（剔除掉 `:` 前缀） 替换 上下文字符串，并且尾部补一个空格。

AI 模式的触发结果：将 用 AI 生成的结果 放在 上下文字符串 后面， 这两个内容要隔一行 。 默认情况下采用非流式请求（可配置）。 如果是思考模式 并且 流式请求的前提下，务必弹出一个合适长宽无边框窗口，将思考过程在这个窗口中滚动呈现，思考完毕后，自动离开这个窗口（不用关闭这个窗口，但可以随时人工回到这个窗口，通过 Esc 键关闭）。然后，进行正式结果输出。

思考过程展示窗口（思考模式 且 流式请求时弹出）还须满足以下人性化要求：
- 1）务必等思考过程**完整展示完毕后**，再输出正式结果（不得在思考过程还在滚动呈现时就提前输出结果，也不得因超时而截断思考展示）；
- 2）思考窗口必须**可拖动**（用户可把它移到不遮挡编辑区域的位置）；
- 3）思考过程中窗口置顶以确保可见；思考完毕自动离开后，窗口**不再置顶**，当焦点回到编辑区域时窗口应被编辑器盖住（不可见但仍存在，除非已被用户手工关闭）；用户可随时通过任务栏回到该窗口并按 Esc 关闭。

  AI 生成的结果有如下规范性要求：

- 1）默认情况下，必须是完整的 latex 片段或范例 （也就是可以合法渲染成数学公式或者符号）；
- 2）如果上下文提示语中有特别的要求，那么返回的结果可以也是 unicode 符号，或者 由 unicode 符号组成的公式。
- 3）如果上下文提示语要求输出的内容是 markdown 格式，那么务必确保 行内公式 用一对 `$` 包围，而行间公式用一对 `$$` 包围。
- 4）如果上下文提示语要求输出绘图代码，那么采用　MikTex + TikZ，并且如果输出包括 `\begin{document}`，那么必须用 用 standalone 文档类；如果是3D输出，那么优先采用 `tikz-3dplot` 宏包，具体根据上下文提示语涉及任务，也可以改用 `pgfplots` 或 纯 TikZ 的 `3d` 库 。
- 5）如果满足上下文提示语要求的生成结果有2种或多种可能，那么务必弹出一个无框列表（相关的要求和后续操作都和前面曾经提及的无框列表是一样的），提供给用户选择。

tikz 模式的触发结果：把上下文内容（选区优先，其次光标前内容）视为 TikZ 绘图代码，自动编译渲染成图片并且默认通过 snipaste 进行贴图（纯本地，不联网；依赖本机已安装的 TeX 发行版与 PDF 转 PNG 工具）。

- 1）渲染链路：pdflatex（MiKTeX / TeX Live）编译 → PDF 转 PNG（自动探测 pdftoppm / mutool / gswin64c / magick，也可在配置中指定）→ snipaste 贴图。
- 2）绘图代码的包装：自动判断上下文内容的形态并包装成最小可编译文档：
  - 包含 `\begin{document}` → 视为完整文档，原样编译；
  - 包含 `\begin{tikzpicture}` → 用 standalone 文档类 + `\usepackage{tikz}` 包装；
  - 仅裸绘图语句（如 `\draw (0,0) -- (1,1);`）→ 额外包一层 `\begin{tikzpicture}...\end{tikzpicture}`；
  - 形如 `\usepackage` / `\usetikzlibrary` / `\tikzset` / `\pgfplotsset` 开头的行会被自动提取到文档导言区（避免写在 document 体内编译报错）。
- 3）编译要求：`-interaction=nonstopmode -halt-on-error`；编译失败时在无框窗口中显示错误信息（取自 main.log 的错误行），不显示图片；编译带超时保护。
- 4）工具路径、渲染分辨率（DPI）、边框留白、附加宏包、编译超时等参数在 config.ini 的 `[tikz]` 段配置；工具路径留空则自动探测（PATH 或常见安装路径）。
- 5）编译期间显示进度提示；结束后自动清理临时文件。
- 6）务必保留触发的调试日志（如果调试开关时开的话）。

play 模式的触发结果：按绑定脚本的**自定义格式脚本**步进式执行（脚本格式与步进语义详见第 8 节）。play 模式无独立开关，其状态由**是否绑定脚本文件**自然决定——已绑定即开启，脚本执行完最后一步自动解绑回到关闭状态；*步进执行命令*（Ctrl+R）在两种状态下均有效。

- **关闭状态下触发**：弹出**系统文件选择对话框**（`FileSelect`，过滤 `*.json`，初始目录 = 脚本 `SharpKnife.ahk` 所在目录）；选定脚本后绑定并进入开启状态，且**立即执行第 1 个动作**。取消选择（空手关闭）则不绑定、无动作；所选脚本顶层动作数组为空（`[]`）时绑定后立即自动解绑、无动作。对话框已打开期间再次触发一律忽略（防重入）。
- **开启状态下触发**：无动作执行中则执行**下一步**（步进游标前移一位）；已有动作执行中则本次**无动作**。play 模式**不提供强制停止 / 解绑命令**，只能执行完最后一步后自动解绑；中途更换脚本须先执行完当前脚本回到关闭状态再重新触发。
- **执行完毕（自动解绑）时**：自动解绑回到关闭状态。
- **失败处理**：脚本加载失败（JSON 无法解析或结构/字段非法）→ 弹出错误提示、**不绑定**（保持关闭状态）；动作执行失败（如文件不存在、ffmpeg / Snipaste 未安装）→ 记日志 / 提示后**跳过该动作**继续前进，不中断脚本；此规则同样适用于 `seq` / `par` 的子动作。

### 8. play 脚本文件格式与动作定义

play 模式的脚本文件是一个 **UTF-8 编码的 JSON 文件**（扩展名 `.json`；存放位置不限，由 *系统文件选择对话框* 指定，且该对话框**只允许选择 `.json` 文件**）。脚本内的相对路径（如 `path`）一律以**脚本文件所在目录**为基准解析。

- 脚本的**根**是一个 **seq（顺序动作）**：顶层是一个动作数组，表示"依次启动"的一串步骤。
- 每个动作都是一个 JSON 对象，用 `type` 字段区分 9 类动作：`text` / `paste` / `audio` / `video` / `seq` / `par`（基础 6 类），外加 `sleep` / `run` / `note`（辅助 3 类，见 8.3）。
- 所有动作对象都可携带一个可选的 `note` 字段（字符串，注释说明，仅作人工阅读，执行时忽略）。
- **未定义的字段一律忽略**（不报错、不校验）：结构校验仅针对已定义字段的必填性与类型，便于日后扩展。
- **数值 / 结构校验总则**（加载阶段整体执行一次）：①**结构非法**——根不是动作数组、动作缺必填字段、字段类型错误、`type` 未知、`pos` / `size` 数组长度不是 2 或元素非数字、`start` / `end` 时间格式非法——一律视为**加载失败**，整体拒绝、不绑定；②**数值越界**——`opacity` 超出 0~100、`volume` 为负、`ttl` 为负——按最近边界**截断（clamp）**，不视为非法。

#### 8.1 各类动作的结构

**（1）text —— 文本输入**

```json
{ "type": "text", "value": "第一行{ENTER}第二行{BS 9}" }
```

- `value`（必填，**字符串 或 字符串数组**）：写入的文本，从**执行动作那一刻的当前焦点窗口**光标处开始。
  - **字符串形态**：`\n` 即换行（单行写法，适合短文本）。
  - **数组形态**：每个元素代表一行，执行时按序拼接并在行间插入换行（适合大段多行文本，避免在一行里堆 `\n`）；数组元素必须都是字符串。

```json
{ "type": "text", "value": ["第一行", "第二行{ENTER}", "第三行"] }
```

- `value` 中形如 `{...}` 的部分按 AHK 键名解释（如 `{ENTER}` 回车、`{BS 9}` 退 9 格），**其余字符（含 `+` `!` `#` `^` `%` 等修饰符）一律按字面原样输出**，不得被 `Send` 解释为修饰键；字面 `{` / `}` 用 `` ` `` 前缀转义（`` `{ `` / `` `} ``）。此规则对字符串与数组的每个元素均适用。

- `delay`（可选，**毫秒**）：字符输出间隔，控制打字速度。缺省 `0` = 即时输出（保持原有行为）；`0` 表示无间隔，正数表示每发出一个字符（或按键动作）后等待 `delay` 毫秒。负值按最近边界截断为 `0`。此字段是**整条 text 动作**的统一间隔，数组的每一行共用。

- **`{Delay N}` 文本内段级延迟记号**（自造扩展，非官方）：在 `value` 的文本流中插入 `{Delay 100}`，可对**同一段文本内部**进行分段控制——从该记号起，**后续所有字符与按键动作**均按 `N` 毫秒间隔输出，直到遇到下一个 `{Delay M}` 切换；记号本身**不输出**。`N` 为非负整数，负值截断为 `0`。
  - 优先关系：`delay` 字段作为整串的**初始**间隔；`{Delay N}` 在运行时**覆盖/切换**。
  - 典型用法——数组内"某些行快、某些行慢"：因 `delay` 是整条统一值，在需要慢速的那行**行首**放 `{Delay 100}`，其余行即时；要切回即时再加 `{Delay 0}`：
    ```json
    { "type": "text", "value": [
        "==================================================",
        "{Delay 100}【play 模式 · 使用教程】本脚本就是教程",
        "================================================={ENTER}"
    ] }
    ```
    也支持同段内部前半即时、后半慢：`"{Delay 0}前半{Delay 100}后半"`（`{Delay 0}` 前无内容时仅确定初始终延迟）。

**（2）paste —— 贴图**

```json
{ "type": "paste", "path": "images/fig1.png", "pos": [100, 200], "size": [400, 300], "opacity": 80 }
```

- `path`（必填）：图片文件路径。
- `pos`（可选 `[x, y]`）：贴图窗口左上角屏幕坐标；缺省水平竖直居中（主显示器）。支持**负值居中语义**：`x < 0`（且 `y >= 0`）→ **水平居中**、竖直位置由 `y` 确定；`y < 0`（且 `x >= 0`）→ **竖直居中**、水平位置由 `x` 确定；`x < 0` 且 `y < 0` → **水平竖直都居中**（等效于未设置 `pos`）。
- `size`（可选 `[w, h]`）：显示尺寸；缺省 = 原图尺寸，指定后拉伸到该精确尺寸（不保持宽高比）。
- `opacity`（可选 0~100）：不透明度；缺省 100，越界按最近边界截断。
- `delay`（可选，**毫秒**）：**延时贴图**时长。缺省 `0` = 贴图动作执行后**立即贴图**；> `0` = 贴图动作执行后经 `delay` 毫秒**才实际贴图**（延迟期间该动作占用执行中标记，脚本不推进）。
- `ttl`（可选，**毫秒**）：**实际贴图后**的延续时长——贴图窗口显示 `ttl` 毫秒后自动销毁。缺省 `0` = **不自动销毁**，由用户自主销毁（保持原有行为）；> `0` = 实际贴图成功后，经过 `ttl` 毫秒自动销毁该贴图窗口。
- `wait`（可选，布尔）：是否等待贴图窗口关闭后才继续。**语义规则**：`delay = 0` **且** `ttl = 0` **同时成立**时 `wait` **无意义**（无论设置为何值都被忽略，始终相当于 `false`）；只要 `delay > 0` **或** `ttl > 0`，`wait` 就**有意义**，缺省 `false`。`wait` 有意义且为 `true` → 该 `paste` 动作在贴图窗口被关闭（`ttl` 到期自动销毁，或用户手动销毁）后才完成，脚本继续；`wait` 无意义或为 `false` → 贴图后立即完成，无须等待。
- `pin`（可选，布尔，缺省 `false`）：**置顶守护**——该贴图在其存活期间始终保持在**本次实际贴图那一刻已存在的全部贴图**之上（如先贴 A 再贴 `pin: true` 的 B，则此后无论用户是否用鼠标点击 A，B 都保持压在 A 上面）。实现：实际贴图成功后，把该贴图窗口句柄与"贴图时已存在的贴图句柄集合"登记进守护列表，由约 100ms 的轮询定时器沿 Z 序链（`GetWindow` + `GW_HWNDPREV`）检查——一旦发现任一登记的被压住窗口跑到该贴图窗口上方（典型场景：用户点击了被压住的旧贴图，Snipaste 将其置前），就用 `WinMoveTop`（不抢焦点、不改位置尺寸）把该贴图提回最前；该贴图销毁（`ttl` 到期自动销毁或用户手动销毁）后自动解除守护，被压住窗口销毁后也不再参与检查。语义要点：①被压住的旧贴图无法再靠点击保持置前（点击后会被守护立即盖回），销毁旧贴图建议使用 `ttl` 或另行方式；②后贴的新贴图不受该守护影响，仍按"新贴图在前"自然叠放（新贴图不在守护的"被压住"集合内）。
- 实现：按 `size` 用 GDI+ 缩放成位图复制到剪贴板，经 Snipaste 贴出；再移动到 `pos`（缺省居中）并 `WinSetTransparent` 设透明度。`delay > 0` 时用一次性定时器在 `delay` 毫秒后执行实际贴图。`ttl > 0` 时用定时器在 `ttl` 毫秒后自动销毁该贴图窗口——销毁方式：短暂激活该贴图窗口并发送 `Esc`（与 Snipaste 官方按 Esc 销毁贴图的交互一致），销毁后恢复原焦点；若 `Esc` 未生效（极端情况）再补发 `WM_CLOSE` / `WM_SYSCOMMAND+SC_CLOSE` 尽力而为。`delay > 0 || ttl > 0` 且 `wait = true` 时，贴图动作以轮询方式等待该贴图窗口关闭后才完成（`wait` 语义见上）。

**（3）audio —— 播放音频**

```json
{ "type": "audio", "path": "audio/bgm.mp3", "start": "00:10", "end": "00:30", "volume": 0.8, "wait": true }
```

- `path`（必填）：音频文件路径。
- `start` / `end`（可选）：起止时间（`HH:MM:SS` 或秒数）；缺省 0 / 播到结尾。
- `volume`（可选）：音量增益倍数；缺省 1.0，负值截断为 0。
- `wait`（可选，布尔）：是否等待播放结束；缺省 `false`（启动即完成，后台继续播）。
- 实现：`ffplay -nodisp` + `-ss` / `-t`（`-t = end − start`）/ `-af volume=...`。`wait = true` 时**非阻塞**等待——启动后立即返回，跟踪 ffplay 进程退出判定完成，进程退出即视为完成（不深究退出原因）。`path` 不存在由 `FileExist` 拦截判执行失败。

**（4）video —— 播放视频**

```json
{ "type": "video", "path": "video/demo.mp4", "pos": [300, 200], "size": [800, 450], "opacity": 100, "wait": true }
```

- `path`（必填）：视频文件路径。
- `pos` / `size`（可选）：播放窗口位置 / 尺寸；缺省居中 / 原始尺寸，指定尺寸后画面在窗口内**等比缩放**（保持宽高比）。`pos` 与 `paste` 一致支持**负值居中语义**：`x < 0`（且 `y >= 0`）→ **水平居中**、竖直位置由 `y` 确定；`y < 0`（且 `x >= 0`）→ **竖直居中**、水平位置由 `x` 确定；`x < 0` 且 `y < 0` → **水平竖直都居中**（等效于未设置 `pos`）。
- `opacity`（可选 0~100）：缺省 100，越界截断。
- `wait`（可选，布尔）：缺省 `false`（启动即完成）。
- `start` / `end` / `volume`（可选）：与 `audio` 完全一致（起止时间、音量增益）。
- 实现：`ffplay` + `-left` / `-top` / `-x` / `-y` 指定窗口位置尺寸（`pos` 含负值时 `-left`/`-top` 不传，启动后读取窗口实际尺寸用 `WinMove` 做居中修正），`WinSetTransparent` 设透明度；`wait = true` 时**非阻塞**等待——跟踪 ffplay 进程退出或播放窗口关闭判定完成。`path` 不存在判执行失败。

**（5）seq —— 顺序嵌套动作**

```json
{ "type": "seq", "step": true, "actions": [ <动作>, <动作>, ... ] }
```

- `actions`（必填，动作数组）：子动作列表；空数组合法但立即完成。
- `step`（可选，布尔）：**单步 / 一次性**。`true`（缺省）= 单步——一次步进命令只执行第 1 个子动作；`false` = 一次性——一次步进命令依次执行完所有子动作。
- 子动作**始终按次序串行**（前一个完成再启动下一个）；`step` 只控制一次推进多少个子动作。
- **继承规则**：一次性 seq 的**所有嵌套子 seq（任意深度）都强制一次性**，忽略其 `step`。

**（6）par —— 并行嵌套动作**

```json
{ "type": "par", "actions": [ <动作>, <动作>, ... ] }
```

- `actions`（必填，动作数组）：子动作列表；空数组合法但立即完成。
- **并行启动**所有子动作，全部完成后该 par 才算完成；par **无单步**。
- **继承规则**：par 的**所有嵌套子 seq（任意深度）都强制一次性**，忽略其 `step`。

#### 8.2 步进（step）执行语义

- 脚本根是 seq（顶层动作数组）。维护两个状态：**步进游标栈**（栈底为顶层游标，指向下一个待执行顶层动作；进入单步 seq 压入其内部游标，该 seq 子动作全部执行完再弹出）与**执行中标记**（当前是否有动作在执行、尚未完成）。初始状态：游标栈仅含指向第 0 个顶层动作的顶层游标，执行中标记为假。
- **绑定即执行第 1 个动作**（无需再按步进命令）；顶层数组为空则绑定后立即自动解绑。
- 每次触发步进命令（Ctrl+R）：
  - 执行中标记为真 → 本次无动作；
  - 否则启动栈顶游标指向的动作：`text` / `paste` 立即执行（期间标记为真，完成前移游标）；`audio` / `video` 依 `wait`（`false` 启动即完成，`true` 非阻塞等待播放结束，期间标记为真）；`seq` 单步（`step=true`）只启动第 1 个子动作并压入内部游标（seq 自身不占标记，子动作全部完成才弹出、顶层游标前移）；`seq` 一次性 / `par` 依次 / 并行执行完所有子动作（期间标记为真，嵌套子 seq 强制一次性）。
- 顶层游标越过最后一个动作 → 执行完毕、自动解绑、回到关闭状态。失败处理同第 7 节。

> 注：`audio` / `video` 是否阻塞仅由 `wait` 决定（与是否嵌套无关），`wait=false` 后台继续播放、不阻塞步进；"播完提示音再打字"可把两者按序放入同一 seq 并给提示音设 `wait=true`。解绑不影响已在后台播放的 `audio` / `video`（继续播完）。

#### 8.3 辅助动作类型（sleep / run / note）

除 8.1 的 6 类基础动作外，`type` 还支持 3 类辅助动作，用于编排演示流程：

```json
{ "type": "sleep", "duration": 1.5 }                        "duration"（必填，秒/时间，HH:MM:SS 或秒数）：等待后继续
{ "type": "run", "path": "C:/tools/x.exe", "args": "-a", "hide": false, "wait": false }
{ "type": "note", "text": "提示文字", "duration": 2.5 }
```

- `sleep`：`duration`（必填，秒数或 `HH:MM:SS`）——睡眠指定时长后继续下一步；非负，负值视为加载失败。
- `run`：`path`（必填，可执行文件 / URL / mailto）——运行外部程序；`args`（可选，参数字符串）、`hide`（可选，是否隐藏窗口）、`wait`（可选，是否等其退出）。`path` 不存在启动失败时记日志 / 提示后跳过（同第 7 节失败处理），不中断脚本。
- `note`：`text`（必填，注释提示文字）+ `duration`（可选，默认 2.5s）——以气泡提示显示文字，到点后继续下一步。
- 上述 3 类的**未定义字段一律忽略，结构 / 数值校验规则与 8.2 一致**（缺必填、类型错、`duration` 非法 → 加载失败整体拒绝；数值越界按最近边界截断）。

#### 8.4 完整示例

```json
[
  { "type": "text", "value": "大家好，欢迎来到演示{ENTER}" },
  { "type": "paste", "path": "images/title.png", "pos": [100, 100], "size": [600, 200] },
  {
    "type": "seq",
    "step": false,
    "note": "先放提示音，提示音结束后自动打字（step=false：一次步进依次执行完；audio 设 wait=true 等播完再打字）",
    "actions": [
      { "type": "audio", "path": "audio/ding.mp3", "wait": true },
      { "type": "text", "value": "（提示音结束）开始讲解" }
    ]
  },
  {
    "type": "par",
    "note": "背景音乐与演示视频同时播放",
    "actions": [
      { "type": "audio", "path": "audio/bgm.mp3", "volume": 0.5 },
      { "type": "video", "path": "video/demo.mp4", "pos": [400, 200], "size": [800, 450] }
    ]
  },
  {
    "type": "seq",
    "step": false,
    "note": "外层一次性 seq：其嵌套的子 seq 即使写 step=true 也强制一次性依次执行完",
    "actions": [
      {
        "type": "seq",
        "step": true,
        "note": "此处的 step=true 被外层一次性 seq 覆盖，实际按一次性执行",
        "actions": [
          { "type": "text", "value": "第 1 段" },
          { "type": "text", "value": "第 2 段" }
        ]
      },
      { "type": "text", "value": "第 3 段" }
    ]
  }
]
```

> 说明：JSON 键名（`type` / `note` / `path` / `value` / `actions` / `step` / `wait` / `pos` / `size` / `opacity` / `pin` / `start` / `end` / `volume`）采用英文（与 `config.ini` 一致）；动作内容（如 `value` 与示例中的中文）可自由使用中文。示例中的 seq 显式写 `"step": false` 表示一次性依次执行完；省略 `step` 或写 `"step": true` 则为单步（默认）——一次步进只执行该 seq 的第一个子动作。`audio` / `video` 省略 `wait` 或写 `"wait": false` 表示启动即算完成，写 `"wait": true` 表示等待播放结束。最后一个动作演示了继承规则：**外层一次性 seq（`step=false`）嵌套的子 seq 即便写 `step=true`，也被强制一次性依次执行完**（par 的嵌套子 seq 同理）。

### 9. AI 模型配置

> 仅对 AI 模式有效

- 1）默认采用官方 Deepseek v4 flash  模型  （可以通过配置修改）
- 2）非上下文的 、对结果起约束作用的 提示语必须是可配置的，但是默认配置必须满足 前面的关于  AI 返回结果的要求。
- 3）接口风格可通过配置选择：默认采用聊天补全接口（chat，DeepSeek 官方默认、OpenAI 兼容）；如所用服务支持，可切换为原生补全接口（completion）。

### 10. 其它要求

- 所有的代码注释 和 程序运行中显示的文字 还有提示语 全部采用中文。
- 在配置中新增调试日志的控制开关，默认情况下不输出调试日志。
